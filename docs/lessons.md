# Lessons Learned

Patterns, mistakes, and non-obvious behaviors discovered during development.
Claude Code appends entries here after sessions where something unexpected happened.
Periodically reviewed and promoted to CLAUDE.md if they represent universal rules.

## Format
- **[YYYY-MM-DD] [BRIEF-ID or context]**: One-line lesson.

## Lessons

<!-- Entries below this line -->
- **[2026-03-31] [B169]**: `user_state.equipment.gyms` usa `gym_id` UUID (assegnato da `_ensure_gym_ids` in onboarding), ma il frontend caricava i gym con cast `{ name, equipment }` perdendo il campo — il dialog passava il nome come gym_id, il resolver non trovava match e usava il fallback (primo gym per priorità). Quando si propagano identificatori tra frontend e backend, verificare sempre che il campo chiave usato dal resolver (`gym_id`) sia incluso nel tipo TypeScript e nel cast dello state.
- **[2026-03-31] [debug produzione]**: Per leggere lo stato di un utente in produzione NON usare `X-Clerk-User-Id` — il backend non accetta quell'header per `/api/state`. Il flusso corretto è: (1) `GET /api/admin/users` con `X-Admin-Key` per listare tutti gli UUID interni con nome/grade, (2) identificare l'UUID corretto, (3) `GET /api/state` con header `X-User-Id: <uuid>` + `X-Admin-Key`. Senza `X-Admin-Key` il backend usa solo Clerk JWT e non è invocabile da curl.
- **[2026-03-31] [B178]**: Gli script diagnostici non devono mai hardcodare path di file catalog/data. Includere sempre uno step di discovery (`find`) prima di qualsiasi script path-dependent. Path hardcodati hanno causato falsi risultati MISSING nell'audit del campo `unilateral`. Si applica a tutte le fasi D-type e B-type.
- **[2026-03-31] [B179]**: Negli script diagnostici su user_state, verificare sempre il key path esatto dalla struttura reale (es. `state["working_loads"]`, NON `state["baselines"]["working_loads"]`). Path annidati sbagliati restituiscono silenziosamente valori vuoti/mancanti e causano falsi allarmi P1. Aggiungere `print(list(state.keys()))` in cima a ogni diagnostica per confermare la struttura prima di scendere nei campi.
- **[2026-03-31] [B174]**: Bug items fixati in un brief ma non marcati Done in ROADMAP_CURRENT.md causano tempo perso a re-investigare lavoro già chiuso. Regola: ogni brief che chiude un item del roadmap DEVE includere uno step che lo marca Done prima della fine della sessione.
- **[2026-04-01] [B182]**: Audit remediation briefs (B165a-e) marked their own rows ✅ Done but left 16 individual P1 findings unmarked in the roadmap. The `/brief` command then reported these ghost findings as open, causing repeated confusion. Rule added: when closing findings via remediation brief, remove each finding from the P1 list — the P1 list IS the source of truth for open items.
- **[2026-04-02] [B185/B186]**: Il soft pattern filter nel resolver droppa quando nessun candidato supera l'equipment check (causa: domain + pattern entrambi non-zeroing). Esercizi con domain generico (es. "power") finiscono in blocchi specializzati per errore. Fix preferito: correggere la classificazione dell'esercizio (cambiare domain), NON il filtro del blocco — meno rischio di effetti collaterali.
- **[2026-04-06] [B189 collision]**: Controllare solo ROADMAP_CURRENT.md per scegliere il prossimo brief number è insufficiente: commit precedenti possono aver usato un numero nel messaggio senza averlo mai aggiunto alla roadmap (es. `c9524de` ha usato B189 senza riga in roadmap, poi un nuovo brief ha riusato B189 credendolo libero). Fix durevole: `scripts/next_brief.py` scansiona roadmap + `git log --all`. Regola aggiunta a CLAUDE.md — eseguire sempre lo script prima di assegnare un numero.
- **[2026-04-06] [D188]**: `trim_roadmap.py` ora verifica dopo la scrittura che ogni ID rimosso da ROADMAP_CURRENT compaia effettivamente in ROADMAP_v2.md, stampando WARNING su stderr se manca. Protegge da perdita silenziosa in caso di regressioni future nel path di archiviazione.
- **[2026-04-06] [D188]**: I commit di sync/counter NON devono portare brief ID nel subject line (pattern come `sync: update test count [B178]` o `C170: sync counters`). Inquinano la reconciliation git-log↔roadmap generando falsi positivi. Usa il body del commit o ometti del tutto il riferimento. `scripts/next_brief.py` ne viene tratto in inganno e può saltare numeri liberi.
- **[2026-04-06] [D188]**: Ogni brief shippato DEVE avere una riga nella roadmap prima della fine della sessione — non affidarsi al trim script per recuperare l'ID dopo. La reconciliation D188 ha dovuto recuperare 16 brief "orfani" (B161, B162, B164, B165, B166, A165, A181, C169, C170, D161, D162, D175, D186, B177, B178, B180, B181, B182) shippati tra il 2026-03-25 e il 2026-04-03 ma mai tracciati. Il commit è la storia, la roadmap è l'inventario — servono entrambi.
- **[2026-04-16] [GTM-04]**: Stripe currency lock su Customer è permanente. Se si cambia valuta del prodotto (es. EUR→USD), vecchi Customer devono essere cancellati manualmente o si usa nuova email. Fix difensivo implementato in subscription.py per auto-recovery da stale customer_id (es. customer cancellato manualmente da Dashboard).
- **[2026-04-18] [A-ACTIVATION-TIMING]**: Non benchmarkare metriche di attivazione contro utenti pre-Stripe. Il drop-off 75% a stage-4 misurato in D-ANALYTICS-DROPOFF includeva utenti che hanno completato onboarding prima del gate Stripe e non avevano obbligo di pagare. Post-Stripe baseline ripartirà da zero: usare D-ANALYTICS-DROPOFF come segnale qualitativo (dove si perdono gli utenti), non come KPI quantitativo da battere. Rifare la misurazione dopo ≥20 utenti post-Stripe.
- **[2026-04-18] [A-ACTIVATION-TIMING]**: In produzione `state.session_completion_log` (JSONB dentro `users.state`) è la fonte autoritativa per contare sessioni completate — NON la tabella `public.session_logs` che risulta vuota. Script diagnostici e retrofit che filtrano per "utenti con zero sessioni" devono leggere `state["session_completion_log"]` via Supabase JSONB, mai fare JOIN su `session_logs`. Verificato sia in `scripts/diagnose_dropoff.py` (funzione `session_counts_from_state`) sia nel retrofit coldstart.
- **[2026-04-20] [B192]**: docs vs codice drift — `session_completion_log` è rimosso attivamente al mark_planned (`replanner.py:383-390`), la roadmap diceva append-only. Solo `feedback_log` + `working_loads` sono genuinamente append-only. Verificare periodicamente che le affermazioni sui log nella roadmap coincidano col comportamento del codice.
- **[2026-04-20] [B192]**: `mark_planned` deve pulire TUTTI i feedback-derived fields, non solo `status`. Ghost state leaks arrivano al frontend da reader senza gate su status. Regola simmetrica: ogni mutazione di `status` deve riconciliare i campi che da quel `status` dipendono. Attento al loop reader-append-only-log: `_attach_feedback` (week.py) leggeva `feedback_log` ad ogni GET e riattaccava i campi al plan — il pop in mark_planned era annullato al primo refetch finché non si è gated su `status == "done"`.
- **[2026-04-20] [B217]**: campo Potemkin survived in docs + 3 frontend senders + 1 backend reader con default. Pattern: se un campo non è mai writer in codice, rimuovilo da TUTTI i consumer (frontend senders + backend readers + docs) in un solo commit. Audit-before-remove richiede grep esaustivo su backend/ + frontend/src/ insieme, non solo un path.
- **[2026-04-20] [A211]**: React 19 lint (`react-hooks/purity` + `react-hooks/set-state-in-effect`) vieta `Date.now()` in render e `setState` sincrono nel body di `useEffect`. `next build` non blocca ma `npx eslint` sì. Pattern corretto nel codebase: `useRef<number>(0)` assegnato dentro effect, e mutazioni di stato time-dependent dentro event handlers / callback, mai nel body di useEffect.
- **[2026-04-21] [A212]**: `@vercel/analytics` v1+v2 dichiarano un `peerOptional @sveltejs/kit` che npm 7+ risolve eagerly anche se Svelte non è nel tree: `@sveltejs/kit`→`@sveltejs/vite-plugin-svelte@7`→vite@8, confligge con vite@7 di vitest. Workaround installazione: `npm install --legacy-peer-deps @vercel/analytics@^1.6.1`. Non è un problema runtime — solo resolver npm.
- **[2026-04-21] [A212]**: brief per pagine di analytics assume spesso un flow che la pagina non ha (`/demo` = reference scrollabile, non start→play→end). Prima di scrivere il codice degli eventi, leggere la pagina reale e rimappare i nomi del brief alla semantica vera. Documentare il remapping nel commit e nella roadmap — altrimenti l'evento chiamato `demo_session_completed` ha semantica opaca rispetto al codice che lo fa partire.
- **[2026-04-23] [B221]**: Parity audits must check the full resolver's emitted-field set against the UI's read-field set, not the audit's find list. D220 initially under-reported the gap because it focused on description/cues only — 4 additional fields (`video_url`, `unilateral`, `alt_sides`, `attributes`) were emitted by the full resolver and read by the guided UI but never surfaced in the audit's delta table. Reopened B221 after investigation found them. Rule: parity audits must list (a) every `instance[...]` assignment in the full resolver and (b) every `exercise.*` access in the UI component, then diff both sides against the light resolver.
- **[2026-04-23] [B224]**: Body-part picker's three-tier role cascade (main → main+accessory → all roles) must be filled tier-by-tier and concatenated, not by pooling eligible roles into one flat list and score-sorting. A flat-pool approach breaks main-preference because `score_exercise()` doesn't penalize accessory/warmup vs main — recency + prefs tie, jitter decides, and main sometimes loses to accessory in tier 2. Tier-first-then-score preserves role ordering while keeping deterministic ranking among peers.
- **[2026-05-04] [B247]**: Wall-clock timer derivation (`Math.ceil(remainingMs/1000)`) + `useEffect([secondsLeft])` gate + 1s `setInterval` is a fragile combination on iOS PWA: any tick drift can land outside the 1-s window and silently drop the beep. Counter-decrement was robust to drift but fragile to backgrounding (the original B67 trade-off). Correct pattern (already in 4 other timers in repo): sub-second tick (200 ms), in-tick check with `lastBeepedSecRef` dedup, reset on every phase transition. Open-threshold transitions (`remainingMs <= 0`) are robust; window-based gates need fine ticks + dedup.
- **[2026-05-05] [B248]**: Manually transcribed conversion tables drift from authoritative sources. The Font→V map in `gradeUtils.ts` was inflated by +1 across 5A..7A vs Wikipedia/Mountain Project standard, undetected for ~4 weeks because tests covered only 7A (which happened to be wrong) and a few other random points. Lesson: any conversion-table data should ship with a parametric test that pins **every** entry against the authoritative source on day one, not just spot-check a handful of values.
- **[2026-05-05] [A-NEW-MACRO]**: Quando un endpoint `/api/macrocycle/generate` poppa flag come `initial_tests_requested` (line 90), un nuovo endpoint che vuole settare quel flag deve farlo DOPO aver chiamato `generate_macrocycle()`, oppure (più pulito) NON passare per `/api/macrocycle/generate` HTTP-style: chiamare direttamente la funzione engine `generate_macrocycle()` e gestire flag/cache/save inline. La seconda strada evita coupling con la lifecycle del flag controllata da un altro handler.
- **[2026-05-05] [A-NEW-MACRO]**: Mappature di grade duplicate inline tra router (es. `_BOULDER_TO_LEAD` in `onboarding.py` linea 369-377) sono un debt nascosto: D232 ha trovato che `PUT /api/state` non applicava la stessa mappatura, lasciando `target_grade` stale dopo un discipline switch via Settings. Estrazione in `backend/engine/grade_mapping.py` con `BOULDER_TO_LEAD` + `LEAD_TO_BOULDER` (highest-boulder-per-lead per non sotto-stimare il livello sui tie) + helper `map_grade_for_discipline_change()` chiude il gap in un solo posto. Pattern: ogni mappatura semantica condivisa tra router DEVE vivere in un modulo engine, non inline.
- **[2026-05-06] [B-NEWMACRO-DEADLINE-FIX]**: `request()` in `frontend/src/lib/api.ts` formatta gli errori come `Error("API ${status}: ${body}")` — il body grezzo finisce nel `.message`. Renderlo direttamente in UI (`<p>{error}</p>`) leak technical detail. Pattern corretto: classifier puro `(err) → {userMessage, status}` con regex `^API (\d+):/`, distingue per status, ritorna copy user-facing; `console.error` per il debug. Refactor di `request()` per error tipato è out-of-scope — il regex contained funziona.
- **[2026-05-07] [B-NEWMACRO-STARTDATE-FIX]**: Decisione di design "evita overlap timeline" (`max(current.end_date+1, today)`) può sembrare conservativa ma in realtà rompe la UX quando l'utente ha un cycle ongoing lungo: clic "Start New Cycle" mid-cycle → date di inizio mesi avanti, utente senza piano in interim. Ogni volta che un dialog UI già comunica "discard / archive", la logica backend NON deve aggiungere wait semantics — l'archive **è** la continuity. Pattern: se la UI dice "ora", il backend deve fare "ora". Helper `strict_next_monday(today)` esisteva già in `start_date_utils.py:43` con la semantica giusta — bastava chiamarlo invece di reinventare `_next_monday_on_or_after` con `max(end+1, today)`.
- **[2026-05-07] [A218]**: Algoritmi di allocazione "default + flex absorber" senza cap superiori convergono su distribuzioni innaturali quando `total_weeks` cresce: lead a 21w produceva 12/4/2/2/1 con base inflated. Pattern corretto: per ogni risorsa allocabile, definire **floor**, **cap**, e una **priority list** per il riempimento del surplus. Il flex absorber prende quello che resta; non può mai superare il proprio cap. Simmetria per shortfall: reduce in inverse priority, never below floor. Senza questa struttura, l'algoritmo è "pseudo-determinista" — output formalmente coerente (sum corretto) ma semanticamente sbagliato.
- **[2026-05-07] [A218]**: Quando un'algoritmica esistente ha due funzioni quasi-duplicate (full vs incremental regen), preferire **una sola funzione con scope parametrizzato** (`phases=...`) rispetto a "due funzioni che condividono helper". I duplicati derivano sempre da costanti hardcoded ribattute (`_compute_remaining_durations` aveva `> 2` invece di `> shrink_floor`) — drift inevitabile. La consolidazione è più sicura del DRY tramite extract-helper, perché elimina il punto di drift invece di nasconderlo.
- **[2026-05-07] [A218]**: Tabelle di verifica scritte a mano nel design doc sono fonti di drift. Tre slip aritmetici nelle mie tabelle boulder (totals 14/15) sono stati catturati dai test entro 30 secondi (assert mismatch atteso vs algoritmo). Lezione: la suite di test è la ground truth, non la tabella; quando si specifica un algoritmo deterministico in un design doc, generare le tabelle dall'algoritmo (anche solo Python a mano) prima di fidarsi delle proprie sottrazioni.
- **[2026-05-10] [D236]**: Commit messages must match the diff. D197 labeled "archived to _archive/" while the diff was pure deletion — caused 4 broken citations and a missing audit doc. Always verify diff before commit when message claims a file move.
- **[2026-05-10] [B-SYNC-FIX]**: Regex anchored su free-prose ("1 app-level health check") fallisce silenziosamente quando il prose evolve ("2 app-level: health check + stripe webhook"). Preferire pattern strutturali stabili (`\d+ endpoints? total`) e sentinel test che asserisce match contro snapshot — il sync no-op silenzioso è invisible per definizione, serve un guardrail post-update che confronta header docs vs counter codice (RC-1 = bug latente per ~3 settimane).
- **[2026-05-10] [B-SYNC-FIX]**: Section-aware doc parser deve fermarsi a QUALSIASI sub-header, non solo a sibling/parent (`^#+\s` non `^#{1,depth}\s`). Una `#### Session-level optional fields` dentro `### 3.0 Canonical session template_ids` faceva leakare un field descriptor (`boulder_fallback`) come fosse un canonical id. Le sub-section non sono mai parte semantica della lista del parent.
- **[2026-05-26] [C239]**: `process_cues.json` ha un escaping non-standard non riproducibile con `json.dump(ensure_ascii=True|False)`: escapa i codepoint > U+00FF (em-dash `—`) ma tiene Latin-1 letterale (`Hörst`). Prima di appendere a un catalog "a mano" sempre verificare la convenzione con un round-trip byte-for-byte (riformatta l'esistente, confronta con il raw) — un append col setting sbagliato sporca il diff su tutte le righe.
- **[2026-05-26] [C240]**: Il numero brief restituito da `next_brief.py` a inizio sessione può essere **stale** se lo snapshot git iniziale precede commit recenti su main (mi ha dato C239 quando era già preso dal merge process_cue). Sempre **ri-eseguire `next_brief.py` o `grep` la roadmap subito prima del commit** — non fidarsi del numero preso in Phase 0. Catturato per un soffio prima di committare un ID duplicato.
- **[2026-05-26] [C240]**: I brief generati dal KB project (claude.ai) descrivono lo schema/struttura in astratto e spesso non combaciano col codice reale: assumevano "un file `.json` per esercizio" (catalog reale = singolo `exercises.json`), `fatigue_cost`/`stress_tags` come stringhe (reali = int / dict fingers-elbow-cns-skin), field `source` inesistente. Trattare il patch KB come **fonte dei dati, non dello schema** — leggere sempre 2-3 entry esistenti del tipo target e replicarne i field esatti prima di scrivere.
- **[2026-06-08] [A221]**: Deploy del codice read-path PRIMA di migrare i dati nel nuovo store. Ho archiviato 17 settimane di Daniele nel cold store mentre prod girava ancora il vecchio main (senza il read-path archivio) → le settimane passate sarebbero diventate `past_week_unavailable` sul live. Rollback immediato (byte-identico), poi sequenza corretta: merge→deploy→canary di verifica (archivia 1 settimana, controlla che il report la legga, ripristina)→migrazione completa. Regola: per ogni feature "sposta dati in un nuovo store + nuovo read-path", il read-path deve essere live in produzione prima del primo write di migrazione. `/health` non espone il commit SHA → usa un canary comportamentale reversibile per confermare la versione deployata.
- **[2026-06-10] [B258/D243]**: Un paywall fail-closed deve distinguere gli stati "mai iniziato" (`none`, `pending_checkout`) da "trial finito" (`past_due`/`canceled`/`expired`) sia nel **messaggio backend** sia nel **banner frontend** — altrimenti chi non ha mai completato il checkout vede "Your trial has ended" (mai avuto un trial) e, se il banner è soppresso per quello stato, resta in un vicolo cieco senza CTA mentre ogni azione 402a. Allineare le due liste (`_NEVER_STARTED_STATUSES` backend ↔ ramo banner frontend). Inoltre: `check_subscription` tratta `trialing` come attivo senza ricontrollare `trial_end`, quindi un webhook Stripe mancato lascia l'utente in accesso gratuito indefinito → serve riconciliazione (proposta in roadmap).
- **[2026-06-14] [D245]**: `BYPASS_USER_IDS` ≠ esenzione pagamento. Il bypass apre solo il guard di accesso (B202), **non** tocca il billing Stripe: un friend&family aggiunto al bypass ma passato dal checkout riceve comunque il trial standard e viene addebitato alla scadenza. Per dare un periodo gratuito davvero: o non far inserire la carta, o applicare un coupon 100%-off al checkout/subscription. Corollario diagnostico: per capire se i webhook Stripe arrivano al backend, controllare `pending_webhooks` sugli `stripe.Event` — un valore >0 su eventi vecchi giorni = delivery mai confermata (2xx) → pipeline rotta (qui: probabile mismatch `STRIPE_WEBHOOK_SECRET`). La riga DB ferma a `pending_checkout` mentre Stripe ha la sub `active`+pagata è la firma di questo guasto.
- **[2026-06-17] [B259]**: `stripe.Webhook.construct_event()` ritorna uno `stripe.Event` (StripeObject), e stripe-python ≥8 ha rimosso `dict.get()` da StripeObject → `event.get("id")` solleva `AttributeError`. Per il codice di parsing webhook usa **bracket access** o converti subito a dict (`to_dict_recursive()`); non usare `.get()` su oggetti Stripe grezzi (event/customer/subscription/invoice). I test mockavano l'evento come `dict` (che ha `.get`) → passavano mentre prod faceva 500 su ogni delivery: un regression test per gli handler Stripe deve mockare `construct_event` con un **vero StripeObject** (`stripe.Event.construct_from(...)`), non un dict. La regressione è arrivata da un **bump silenzioso**: tutte le dipendenze in `backend/requirements.txt` erano unpinned → pinnare almeno le librerie su path critici (billing). Diagnosi via `railway logs` (la riga del traceback indica l'esatto file:line) + POST firmato col secret di Railway per isolare "firma" da "handler".
- **[2026-06-17] [B259]**: Rifare (resend) un webhook Stripe **non genera mai un addebito** né tocca coupon/sconti — è solo il replay di una notifica. Ma NON è sempre idempotente lato nostro: `_handle_checkout_completed` forza `status="trialing"` con le date trial originali, quindi ri-inviare un vecchio `checkout.session.completed` a un utente ora `active` lo **declasserebbe**. Per allineare una riga già corretta non rifare gli eventi: o riconcilia direttamente il DB (Stripe è source-of-truth) o rifai solo l'evento che porta allo stato giusto.
- **[2026-06-19] [C241/OPS]**: The session-start git snapshot is a point-in-time capture and can be stale — it reported `feat/A223-plan-pause` while the live checked-out branch was `brief/A224-weather-today`, so C241 commits landed (and were pushed) on the wrong branch. Wrong-branch commits on a branch under preview review create divergence and future merge friction. Rule: run `git branch --show-current` immediately before ANY commit/push — never trust the session-start snapshot. Keep the convention: backend-only work → push to `main`; only frontend work uses a `brief/` branch.
- **[2026-06-19] [B260]**: A hardcoded `ex_id != "..."` exclusion in a data-driven path turns the catalog config into **dead code**. `limit_bouldering` carried correct `grade_ref: boulder_max_rp` / `grade_offset: 0` in the catalog, but `inject_targets` excluded it from the catalog grade path and re-derived the target from a parallel board-benchmark source — so the (correct) catalog values were never read and the target silently diverged (7A vs 7C). When auditing a "wrong value" bug, don't stop at the config: verify the config is actually *consumed*. A grep for `ex_id ==`/`ex_id !=` near the derivation reveals special-case bypasses fast.
- **[2026-06-19] [A225/D1]**: The "filter by equipment, not location" rule applies to **session/exercise filtering**, not to **venue detection**. An outdoor day is legitimately detected via `availability.*.location == "outdoor"` because "outdoor" is a venue with no indoor-equipment profile to gate on — the planner reads the flag only to *exclude* the day from indoor assignment (`day_is_outdoor`), never to filter a trainable session. Documented as a standing exception in `vocabulary_v1.md §1.1`; needed no `planner_v2` change. When a new feature seems to "violate" the equipment-vs-location principle, first check whether it's filtering a session (rule applies) or detecting a context (exception).
- **[2026-06-19] [A225]**: Catalog vocabularies must match the **engine** vocabulary, not the KB/source doc's. C241 authored the outdoor strategy catalog's `macrocycle_phase` patches with KB names (`build`, `peak`) while the engine emits `strength_power`, `power_endurance` — forcing a translation layer in the resolver. Fixed by renaming the catalog keys to the engine vocabulary so lookup is direct (one vocabulary, no mapping). When a new catalog keys off an existing engine enum, copy the enum's exact values from the engine, not from the brief/KB prose.
- **[2026-06-19] [B261]**: When an explicit `exercise_id` pin is made to fall back to filter-based selection (P0) on equipment-incompatibility, that fallback is **dangerous for test/measurement blocks**: P0 by `role=test` will happily substitute a *different* test (a hangboard max-hang pin → `test_hip_flexibility`), silently measuring the wrong axis. A pinned measurement is semantically "this exact exercise or nothing" — on incompatibility it must **skip**, never substitute. Guard via `role contains "test"`. General rule: a generic fallback that's correct for substitutable blocks (warmups, accessories) can corrupt non-substitutable ones (tests) — classify the block before delegating.
- **[2026-06-19] [B261]**: `compatible_with_location()` in `resolve_session.py` reads `ex.get("equipment")` (a near-unused field) and ignores BOTH `equipment_required` and `equipment_required_any` — so it would NOT have caught the `warmup_easy_boulders` leak (which gates via `equipment_required_any`). The real equipment gate lives inline in `pick_best_exercise_p0` Stage 2. Before reusing a plausibly-named predicate, confirm it reads the field your case actually uses — name ≠ behavior.
- **[2026-06-20] [B266]**: A Safari/iOS "Load failed" (a `fetch()` TypeError, not an HTTP status) on a write usually means the request got **no CORS-valid response** — most often a backend **500 whose error response lacks CORS headers**, so the browser masks the real status. Don't trust the client string: capture the server traceback. Here the 500 was `postgrest APIError 23505` — a Supabase `.upsert()` **without `on_conflict`** defaults to the serial PK as conflict target, degrading to an INSERT that violates a `(user_id, X)` unique constraint on the second write. Always pass `on_conflict="<unique cols>"` when the table's uniqueness is NOT its PK. Repro tip: the bug only appears on Supabase with a **pre-existing row** for that key — file backend and fresh test users both pass; reproduce against the real account (or seed the conflicting row).
- **[2026-06-21] [A223]**: For a date-shift feature (plan pause/resume), shifting the immutable anchor (`start_date`) is the *wrong* lever even when it looks cleaner: completed past weeks fall below the new anchor and get **expelled** from the `[start_date, end_date]` completion window (`macrocycle_archive`). Option B — keep `start_date` immutable, add a cumulative offset read ONLY by the forward-position consumers (the 2 in `deps.py`), extend `end_date` — preserves past accounting by construction. Measure the offset **Monday-to-Monday** (always ×7) so the Monday-invariant holds for free; the intra-week partial is already handled by the planner's `today` param.
- **[2026-06-21] [A223]**: "Paused" vs "missed" is a **display-only, read-time derivation** from the pause log — never mutate a session record to mark it paused (that would break past-session immutability). Same principle as B3 in A227: classify at render, don't rewrite history. Recency/closed-loop easing after a layoff is *correct* behavior and was deliberately left untouched.
- **[2026-06-21] [A223]**: A long-lived feature branch can fall far behind `main` (here 54 commits / a new `weather` router landed meanwhile). `git rebase main` cleanly auto-merged non-overlapping router registrations — but always re-run the FULL suite after the rebase, and isolate any new red by re-running it at the branch-point commit *today*: a date-dependent failure (Sunday) that reproduces at the old commit with identical code is pre-existing, not your regression.
- **[2026-06-26] [B267]**: Silent variety-death from future-week saturation: `load_recent_exercise_ids` took the N **newest** `week_plans` keys by date, but the hot store also holds PRE-GENERATED FUTURE weeks (`status != done`). With ≥N future weeks pre-generated, the recency window filled entirely with future placeholders → `recent_ex_ids = []` → every selection block fell to the alphabetical tie-break (anti_rotation → always `copenhagen_plank`, never `pallof_press`). The user saw "same exercises every time, never X". Fix: filter `key > current_week_monday` (a `reference_date`, default today) BEFORE the top-N slice AND before the "<N hot → consult archive" decision (so that decision counts PAST weeks only). General rule: a "most-recent-N-keys" window over a store that contains BOTH past and future entries is a latent bug — bound it by `<= now`, and verify the empty-recency path isn't masking dead variety (an empty recency list looks innocuous but disables all rotation).
- **[2026-07-03] [B271]**: Fixture con date assolute rottano in silenzio: `test_user_state.json` aveva `goal.deadline=2026-06-30` hardcodata → dal 2026-07-01 il guard "deadline in the past" di `/api/macrocycle/generate` ha fatto fallire 20 test su main senza alcun cambio di codice. Le date nelle fixture condivise vanno messe lontane anni (o iniettate dinamicamente); quando la suite si rompe "dal nulla", controllare per prime le date hardcodate confrontando lo stesso commit su cui ieri era verde.
- **[2026-07-11] [A230]**: Quando un brief contraddice se stesso su un numero (prose "31 entries" vs tabelle con 35 righe e region counts che sommano a 35), la fonte di verità sono le tabelle + la spec dei test che concordano tra loro — implementa quelle, documenta il refuso nel report, non "aggiustare" il pool per far tornare il numero in prosa.
- **[2026-07-11] [A231]**: Quando aggiungi una feature dello stesso genere di una esistente (mobility ↔ Core Circuit), copia il MODELLO DI INTERAZIONE esistente, non solo l'architettura dati: A230 aveva backend solido ma UX browse-and-tap entry-per-entry, bocciata subito ("il sistema sceglie gli esercizi, io scelgo tempo e regioni"). Il pattern setup→auto-flow era già in CircuitSetup/CircuitTimer da studiare prima di disegnare la UI, non dopo il feedback.
- **[2026-07-11] [B273]**: I side effect che chiudono il closed-loop (status/load/ripple sul week plan dopo un log) non devono vivere nel client: l'endpoint finish (A225) scriveva il log ma il flag `complete_outdoor` lo mandava solo il frontend del flusso legacy → ogni nuovo entry point divergeva in silenzio (vie invisibili, ripple mai applicato). Il backend che registra l'outcome deve anche chiuderlo; se un invariante esiste solo come coincidenza di due chiamate frontend, scrivilo in un test.
- **[2026-07-12] [B-COACH-CONTEXT-FIX]**: La suite completa muta `backend/data/user_state.json` locale (un test non isolato rigenera `macrocycle.generated_at`) → il pre-push hook blocca il push per file dirty; `git checkout -- backend/data/user_state.json` prima del push, e a monte andrebbe trovato il test che sfugge all'isolamento B216.
- **[2026-07-13] [founder-alerts]**: Le env var documentate in CLAUDE.md non implicano che siano davvero impostate su Railway: TELEGRAM_BOT_TOKEN/CHAT_ID erano solo in `.env` locale da A222 (aprile) → `notify()` no-op silenzioso in prod per 3 mesi, nessun alert su nuovi iscritti mai partito. Dopo aver aggiunto una env var, verificare SEMPRE con `railway variables --json`; i design "silent no-op se unset" nascondono le misconfigurazioni — meglio loggare un warning allo startup.
- **[2026-07-13] [C256]**: Prima di committare, verifica sempre il branch corrente — un'altra sessione può aver lasciato il repo su un feature branch (commit finito su brief/A232-trial-nocard, recuperato con cherry-pick su main + branch -f al commit originale).
- **[2026-07-13] [B274]**: Nei selettori deterministici il tie-break alfabetico/posizionale affama silenziosamente il contenuto che ordina in coda (tutti i drill `tech_*` fuori rotazione per mesi): la correttezza di una singola risoluzione non basta, testare la varietà su settimane simulate; un hash con seed temporale mantiene il determinismo eliminando il bias.
- **[2026-07-13] [A232]**: Con due sessioni Claude concorrenti sullo stesso repo, `git stash` + checkout main per bisecare un test è fragile: il pop può conflittare su `backend/data/user_state.json` (mutato dalla suite) e abortire SENZA applicare gli altri file — le modifiche sembrano perse ma sono nello stash. Recupero: `git checkout stash@{0} -- <paths>` mirato + reset di user_state.json da HEAD. Meglio: committare (anche WIP) prima di qualsiasi checkout incrociato.
- **[2026-07-13] [B274-followup]**: `pytest | tail` restituisce l'exit code di tail, non di pytest — 5 failure spediti credendo la suite verde. Verificare SEMPRE senza pipe (o con pipefail/redirect su file); e i test che dipendono da QUALE esercizio vince un tie-break vanno scritti discover-then-assert, non con id hardcoded.
- **[2026-07-13] [B275]**: Stripe emette `invoice.payment_succeeded` anche per la invoice $0 di apertura trial — qualsiasi handler "pagamento riuscito" deve skippare gli importi zero, altrimenti promuove trialing→active e nasconde banner/countdown; e sulle API Stripe recenti l'invoice non ha più `subscription` top-level → mai fare upsert di un id con valore None. La verifica E2E in prod (non solo i test) ha scovato entrambi.
- **[2026-07-13] [A233/trim]**: `trim_roadmap.py` riconosceva solo bullet `- ✅ …`, ma le entry "Recently closed" usano `- **ID — titolo** ✅ …` → 55 entry (75 KB) accumulate in silenzio con dry-run che riportava 0. Quando cambia il formato delle entry di un documento mantenuto da script, verificare subito che lo script lo riconosca (un dry-run che dice sempre 0 è un sintomo, non una conferma).
- **[2026-07-19] [A234]**: Brief misto con endpoint backend NUOVI: la preview Vercel chiama il backend di PROD, quindi la parte frontend è invisibile finché gli endpoint non esistono su main. Pattern: pushare prima la slice backend-only additiva su main (lecito per B196), poi testare la preview end-to-end; mergiare main nel branch subito per evitare conflitti sui counter sync.
- **[2026-07-20] [D253]**: Un brief può portare una premessa falsa ("BUG-2: coach risponde in italiano nonostante l'enforcement English-only") — ma nel codice non esiste alcuna regola English-only, l'istruzione dice "rispondi nella lingua dell'utente". Verificare SEMPRE la premessa di un bug contro il codice reale prima di scopare un fix, o si "aggiusta" una regola inesistente. E il weather del coach NON passa dal router L3: è pre-fetch iniettato sempre nel context block (trigger su lat/lon + outdoor day, mai sul testo) → una domanda meteo in italiano oggi non fa partire nessun fetch.
- **[2026-07-20] [B281]**: Quando aggiungi all'app una capability che l'LLM "rappresenta" (la card adhoc A243), aggiorna il suo instruction block NELLO STESSO brief — il prompt A237 diceva ancora "non puoi creare sessioni" e il coach ha negato e si è scusato per una card valida. E l'estrazione intent su singolo messaggio perde i vincoli dei turni precedenti ("riprovi?" → tutti default): passare sempre una finestra di conversazione.
- **[2026-07-20] [B282/B284]**: Un gate regex a lista di coniugazioni perde sempre contro la morfologia italiana ("mi prepari?", "crei", "manda") — usare stem + co-occorrenza nome/verbo a livello messaggio, biased verso il routing (il falso positivo costa un'estrazione che fa fallback). E con più palestre registrate, "gym" senza nome = unione di TUTTE le attrezzature: qualsiasi slot-spec di luogo deve catturare il nome verbatim e fare match sull'elenco dell'utente.
- **[2026-07-20] [B285]**: Un fallback "dev/test" (`X-User-ID` senza env check) è un IDOR completo appena il servizio va in produzione: ogni scorciatoia di auth va gated su una condizione d'ambiente esplicita (`ALLOW_LEGACY_HEADER`), default fail-closed, mai "tanto in prod nessuno la usa". Corollario: il vero fix era a monte (401 quando manca la credenziale) invece di tappare i singoli consumer a valle — e mascherava anche un bug di correttezza (token scaduto → dati del bucket `__legacy__` invece di un errore). E prima di rendere fail-closed un bucket condiviso, CONTARE le righe reali in prod (erano 0) invece di assumerlo.
- **[2026-07-20] [B285]**: `pyproject.toml` ha già `addopts = "-q"`, quindi `pytest -q` diventa `-qq` e **sopprime la riga di summary** ("N passed") — sembra che la suite non abbia girato. Usare `-o addopts=""` per vedere il conteggio; l'exit code resta l'unica verità.
- **[2026-07-20] [B286]**: Brief-bugfix (B-COACH-CONTEXT-FIX v2) aperto per fixare "BUG-1: coach cieco ai giorni outdoor" — ma BUG-1 era già chiuso dal B-COACH-CONTEXT-FIX originale (`c7c4b58`, "outdoor days visibility" via `_day_extras`) E ri-confermato da D252. Anche BUG-2 già chiuso dallo stesso commit (English-only → match-user-language). Prima di scrivere codice su un bug citato da un brief, cercare le chiusure precedenti (roadmap "Recently closed" + `git log --all --grep`): la premessa "letto solo da X" era una sovra-generalizzazione (un secondo reader `_day_extras` già lo esponeva). Il STOP-and-report del Phase 0 ha evitato lavoro ridondante.
- **[2026-07-20] [A244]**: Migrare il meteo del coach da pre-fetch a native tool use ha toccato un solo punto d'innesto (`llm_client.chat` + `service.handle_chat`) perché il tool use forzato esisteva già (A243 `extract`). Chiavi: (1) il tool definition va nel prefix cached (order tools→system) → costo ≈0 se schema byte-stabile, ma NON metterlo dietro un gate keyword o si reintroduce il blind-spot English-only del router L3; (2) l'executor NON deve MAI sollevare nel loop (provider fail → stringa "unavailable", il modello lo dice); (3) servono due cap distinti — MAX_TOOL_CALLS (costo) + MAX_ITERATIONS backstop (terminazione garantita anche con un modello avverso che chiede tool all'infinito); (4) quando cambi la firma di una funzione mockata nei test (`llm_client.chat` ora ha `tools`/`tool_executor`), aggiorna la fixture mock ad accettare **kwargs o ogni endpoint test che la usa esplode.
- **[2026-07-20] [B287]**: Ogni path che muta un week plan deve passare da preserve/reconcile: `set_availability` rigenerava la settimana e riassegnava `updated["weeks"]` senza `regenerate_preserving_completed`, distruggendo le sessioni done/skipped, ed era l'unico event type che rigenera ma l'unico endpoint senza guard B257 (`/events` non l'aveva, `/override` sì). Corollari: (1) qualsiasi nuovo event type che rigenera VA aggiunto a `_REGENERATING_EVENT_TYPES` — auditare quella lista ogni volta che `generate_phase_week` guadagna una call site; (2) un guard past-week blanket avrebbe rotto il `mark_done` retroattivo, che è *explicit user edit*, l'unica eccezione ammessa dal pilastro — restringere il guard alla rigenerazione, non all'endpoint; (3) un fallback "match per giorno della settimana" che riscrive la data fabbrica storia di allenamento e fa attaccare il feedback sbagliato via chiave (date, session_id): sotto il preserve floor solo match a data esatta.
- **[2026-07-20] [B287]**: Un default di comodo su un parametro (`_build_session_pool(phase_id, discipline="lead")`) si propaga silenzioso: il replanner leggeva `discipline` dallo snapshot per i pesi e la dimenticava la riga dopo per il pool → pesi boulder + pool lead. Blast radius reale più largo dell'ipotesi (5/17 utenti, non 1: `both`/`all_round` hanno un pool a sé, non ricadono su lead). E se un enforcer deterministico corregge una cosa che l'utente ha chiesto esplicitamente, deve restituire un record machine-readable del perché (`adjustments: [{action, reason, previous_session_id}]`) — enforcement sì, silenzioso mai.
- **[2026-07-20] [B287]**: Verificando un'asimmetria voluta (le sessioni `done` della settimana precedente contano come seed per il gap dita) è emerso che la stessa domanda **specchiata** dentro la settimana aveva risposta sbagliata: `status != "done"` era usato per l'INTERA computazione, quindi una sessione dita allenata martedì non vincolava il mercoledì **e** veniva riscritta dal loop di declassamento (violazione di immutabilità dentro il modulo che doveva proteggerla). Lezione: separare sempre "cosa vincola" da "cosa è modificabile" — sono due predicati diversi (done = vincola, mai bersaglio; skipped = né l'uno né l'altro), e un singolo flag di stato usato per entrambi produce un buco su un lato e un danno sull'altro.
- **[2026-07-20] [hygiene]**: Una suite può fallire "da sola" senza che il codice cambi: i test endpoint coach scrivono nel JSONL condiviso `backend/data/logs/` (gitignored → invisibile a `git status`) perché la fixture isola solo `STATE_PATH`; al 30° messaggio utente nello stesso giorno UTC scatta `DAILY_MESSAGE_LIMIT` e 3 test danno 429. Si auto-risolve a mezzanotte, il che maschera la causa. Regola: prima di dare la colpa al proprio diff, controllare se il test dipende da stato accumulato fuori da `tmp_path` — e isolare *tutti* i path di scrittura nella fixture, non solo quello dello state.
- **[2026-07-20] [A245 Phase B]**: Una convenzione di chiave localStorage che *sembra* scopata per utente ma non lo è (`guided_session_${userId}_` dove `userId` era la stringa **letterale** `"clerk"`) resta innocua finché tiene solo dati effimeri — e diventa una data leak nel momento in cui qualcuno persiste dati veri sotto la stessa convenzione: dopo un cambio account o un import da recovery-code il piano di A viene renderizzato a B. Regole: (1) ogni chiave di storage per-utente porta l'id **reale** e va verificata, non assunta; (2) i dati legacy non-scopati si **purgano**, non si migrano — ri-attribuirli all'utente loggato È la leak che si sta chiudendo; (3) il purge va agganciato al *cambio* di `userId`, non al sign-out: il cambio account è il caso pericoloso e il sign-out non ha sempre un callback applicativo (con Clerk `<UserButton />` non ce l'ha).
- **[2026-07-20] [A245 Phase B]**: Architettura offline — non tutto ciò che fallisce offline può essere messo in coda. Il discriminante è **append-only e autoconsistente**: un feedback o un log outdoor nuovo sì, un `PUT` di modifica no (riprodotto ore dopo sovrascrive quello che è cambiato nel mezzo), e un evento che spedisce **l'intero week plan** meno che mai (il backend lo persiste wholesale → lost-update illimitato nel tempo + rischio sulle sessioni passate immutabili). Ciò che non è accodabile deve fallire **rumorosamente**, non in silenzio. Corollario di React Query: persistere una query senza alzarne il `gcTime` ad almeno `maxAge` non serve a niente — l'evict a 5 minuti dall'ultimo observer fa sì che il persist successivo riscriva la cache **vuota**.
- **[2026-07-20] [A245 Phase B]**: Nel service worker, `request.mode === "navigate"` **non** intercetta le navigazioni App Router: sono fetch RSC, quindi non entravano mai in cache e offline la bottom nav non faceva nulla, senza feedback. Vanno riconosciute a parte (header `RSC: 1` o `?_rsc=`). Altre due trappole nello stesso file: `cache.addAll` è all-or-nothing (un solo 404 aborta l'intero precache → utente senza nessuna cache), e un `network-first` senza timeout non è network-first ma "attendi il timeout TCP" — su rete flappante sono decine di secondi di schermo bianco.
- **[2026-07-20] [A245 Phase C]**: Un `catch` che non guarda *quale* errore è arrivato trasforma "non lo so" in "so che non c'è". Tre istanze dello stesso bug nello stesso brief: errore di rete → "non abbonato" (rimbalzo a `/subscribe` di un pagante in palestra), errore di rete → "nessun log outdoor per questo giorno" (form nuovo aperto su una giornata che ne aveva già uno → duplicazione/sovrascrittura), auth Clerk fallita → richiesta inviata senza header (che il backend legge come utente nuovo). Regola: prima di degradare uno stato di business, chiedersi se l'errore è **una risposta del server su quel dominio** (401/402/403 sull'entitlement, 404 sull'esistenza) o **trasporto** (0/408/429/5xx). Solo il primo è evidenza. Corollario fail-closed: la distinzione va combinata con "abbiamo mai avuto una risposta autorevole?" — prima della prima risposta non si può distinguere *entitled* da *unknown*, quindi si nega comunque.
- **[2026-07-20] [B288]**: Due sessioni Claude sullo stesso repo condividono HEAD, indice e working tree: la seconda che fa `git checkout -b` si porta dietro il lavoro non committato della prima, e il primo `git add -A` dell'altra sessione impacchetta file altrui in un commit con il messaggio sbagliato, sul branch sbagliato (qui: il fix backend di B288 finito dentro "A245 Phase D"). Regole: (1) ogni sessione parallela in un **git worktree** dedicato (`git worktree add ../repo-<brief> brief/<id>`); (2) mai avviare un brief in un tree con modifiche non committate — controllare `git status` *e* `git branch --show-current` prima di ramificare, perché "sono su main" è un'assunzione, non un fatto; (3) districare si fa solo a sessioni ferme, con un branch di salvataggio sul commit misto e backup su disco dei file non committati **prima** di qualsiasi operazione git.
- **[2026-07-20] [B288]**: Un gate di freschezza può fare due lavori diversi con lo stesso `if`. `_is_fresh` scartava le entry vecchie **e** quelle senza data valida o datate nel futuro: passare `freshness_days=None` per allargare il primo comportamento ha silenziosamente disattivato anche il secondo, resuscitando un bug che aveva già un test dedicato (B-fix-CORE). Per allentare un vincolo, allargare il parametro (finestra lunga ma finita) invece di bypassare il controllo — e quando i test si rompono, separare sempre "guard rail che ho rotto per sbaglio" da "comportamento che il brief cambia di proposito": qui erano 2 e 2, e trattarli allo stesso modo avrebbe spedito il bug in produzione.
- **[2026-07-20] [A245 Phase D]**: Un bottone `disabled` non dà **nessun** feedback al tap: l'utente non sa nemmeno di aver interagito e va a caccia nella pagina di cosa è cambiato. Lasciar passare il tap e rispondergli («Before you continue: …») trasforma un vicolo cieco in un'istruzione. Corollario emerso dalle catture: cercando i bottoni grigi si trova anche l'inverso — `locations` **non** era grigio e lasciava passare con zero superfici di arrampicata mentre il copy prometheva il contrario, quindi il piano nasceva senza arrampicata e lo si scopriva dopo. Quando si auditano i gate, misurare `isDisabled()` su **tutti** gli step: sia i falsi negativi (grigio ingiustificato) sia i falsi positivi (permissivo ingiustificato) sono lo stesso bug — validazione e promessa che divergono.
- **[2026-07-20] [A245 Phase D]**: Prima di rendere opzionale un campo di onboarding, **leggere il consumatore nel motore**, non assumere. L'onsight sembrava obbligatorio per il calcolo del gap RP/OS, ma `assessment_v1` aveva già `else: gap_score = 50.0` sia in `_compute_power_endurance` sia in `_compute_technique`: era opzionale da sempre lato engine e obbligatorio solo lato form. Costava abbandono e non comprava nulla — zero righe di backend per rimuoverlo.
- **[2026-07-20] [A245 Phase E]**: Due chiavi diverse con lo stesso nome (`adjustments` scritto da `adaptation/closed_loop.py` vs la lista `adjustments[]` che il replanner ritorna da B287) fanno concludere a chi grepa che il codice morto sia vivo: si trova un match in codice di produzione e si smette di cercare. Prima di dichiarare "questo è collegato", risalire al **lettore** di quella specifica struttura, non al nome. Corollario opposto e altrettanto insidioso: `cooldowns.per_cluster` ha un lettore vivo con due comportamenti reali, ma **zero writer** — quindi la feature è documentata, testata e mai eseguita. Dead code può stare su **entrambi** i lati della freccia.
- **[2026-07-20] [A245 Phase E]**: Una dependency di sicurezza può essere un no-op sotto pytest e nessuno se ne accorge: `require_active_subscription` è disattivata quando manca Stripe o il backend non è supabase, quindi aggiungerla a un endpoint **non rompe niente** — e "i test passano" non dimostra che il gate esista. Per i gate bisogna patchare il controllo (`check_subscription`) e asserire il 402. Stessa famiglia: rivedendo il diff di un commit che doveva toccare 1 file ne ho visti 2, e il secondo era `backend/data/user_state.json` scritto dai test veri. **Leggere sempre il `--stat` del proprio commit prima di committare** — i file di troppo raccontano bug che i test verdi non raccontano.
- **[2026-07-20] [B290]**: **Un fix di chiave applicato a metà è peggio di nessun fix.** In A245 B-2 ho corretto il letterale `"clerk"` in `guided-session-utils.ts` e in `today`, ma non negli altri due produttori (guided page, session-card): writer e reader hanno smesso di parlarsi in silenzio. Il flusso felice restava intatto — il player scriveva e rileggeva con la propria funzione, quindi la preview passava — mentre resume, retry del feedback offline e persistenza fra aperture erano morti, e il purge delle chiavi "legacy" cancellava le sessioni **vive** che quei writer continuavano a produrre. Regole: (1) tutti i produttori di una chiave si correggono **nello stesso commit**, mai uno alla volta; (2) se una chiave è costruita inline in più punti, l'unificazione **precede** il cambio di formato; (3) serve un test *strutturale* (grep sul sorgente, commenti esclusi) oltre a quelli comportamentali — un nuovo builder inline passerebbe tutti i test di comportamento e romperebbe la produzione allo stesso identico modo.
- **[2026-07-20] [B290]**: Una checklist di preview che verifica solo il percorso felice **dentro una sessione** non può vedere i bug di persistenza. Ogni feature che scrive su localStorage/IndexedDB va provata con **chiudi-e-riapri**: avvia → chiudi l'app → riapri → lo stato c'è ancora? E una seconda volta, per scoprire le migrazioni non idempotenti. Il bug delle chiavi guided è passato dalla preview di Phase B proprio perché quello scenario non era in lista.
- **[2026-07-20] [B291]**: Un'obiezione tecnica plausibile va **verificata**, non ereditata. B-TEST-COACH-ISOLATION non redirezionò `STATE_PATH` perché «un override globale litigherebbe con i moduli che si isolano da soli» — sembra ragionevole e ha lasciato aperto per mesi un leak che sporcava un file tracciato a ogni run. In realtà `monkeypatch` è function-scoped: la fixture autouse del conftest e quella di modulo condividono lo **stesso** oggetto monkeypatch, l'autouse gira per prima e la patch di modulo vince, con teardown in ordine inverso. Un redirect globale è un *default*, non un override. Costo della verifica: cinque minuti e un test.
- **[2026-07-20] [A245 Phase F]**: Una classe CSS assemblata a runtime non esiste. `ring-${opt.color.replace("bg-","")}` sembrava funzionare — il chip cambiava comunque sfondo — ma Tailwind scansiona il **testo sorgente**, quindi quella classe non è mai stata generata e il ring di selezione non c'è mai stato. Regola: ogni classe Tailwind va scritta per intero da qualche parte nei sorgenti (nei dati, in una mappa, in una safelist); se la componi con template string, il compilatore non la vede. Vale anche per il rovescio: un test che grepa i sorgenti va fatto **dopo aver spogliato i commenti**, altrimenti matcha la prosa che descrive il bug (ci sono cascato due volte in questa fase).
- **[2026-07-20] [A245 Phase F]**: Un duplicato sopravvive a un grep del proprio nome se le copie hanno **nomi diversi**: `formatDateShort` esisteva in cinque copie byte-identiche, tre col suo nome e due chiamate `formatDateLabel`. Cercare per *corpo* (o per costante caratteristica, qui l'array dei mesi) prima di dichiarare "una sola copia". E il contrario: prima di fondere due cose che *sembrano* duplicate, confrontarle davvero — `FEEDBACK_OPTIONS` in `day-card` ha 3 valori e classi diverse dalle 5 difficoltà, unificarle avrebbe rotto i badge delle attività complementari.
- **[2026-07-20] [A245 Phase F]**: Verificare la stima di un audit prima di dimensionare il lavoro. F26 diceva "timer che ri-renderizzano ~1000 righe a 1 Hz": misurato, il return è 368 righe senza sotto-alberi pesanti, e il costo maggiore era un altro (ogni keystroke negli input adiacenti ri-renderizzava il timer, risolto con `memo`). F27 diceva "spostare ClerkProvider": misurato, Clerk serve a tutte le route tranne tre, e isolarlo richiede di spostare la root in un route group — valore reale concentrato su `/demo`, costo strutturale. Un numero nell'audit è un'ipotesi da confermare, non un requisito.
- **[2026-07-20] [B292]**: Precachare una route **autenticata** non funziona: al momento dell'install il service worker riceve un 404/redirect dal middleware e la `cache.add()` fallisce. Se poi il fallimento è ingoiato per-URL, l'install "riesce" con una cache vuota e la PWA non si apre offline — cioè il fallimento è totale ma silenzioso. Regole: (1) nel precache solo documenti **pubblici**, le pagine dietro auth si cachano a runtime alla prima visita da loggati (che basta: non puoi averne bisogno offline prima di averle aperte online); (2) la pagina di fallback offline **deve** essere pubblica, altrimenti non può entrare in cache e non potrà mai essere servita; (3) un fallimento di precache va loggato come **errore**, non come warning. Corollario sul matcher Next: `js(?!on)` esclude `.js` ma **non** `.json`, quindi `manifest.json` finiva dietro auth mentre `.webmanifest` sarebbe passato.
- **[2026-07-20] [B292]**: `isLoading` di React Query è **false** appena esiste un valore in cache, anche vuoto o restaurato da localStorage. Dopo aver aggiunto la persistenza (A245 B-2), `/today` mostrava "Welcome! Completa l'onboarding" a un utente con mesi di allenamenti, perché la `state` vuota persistita restaurava con `isLoading === false` mentre il fetch vero era ancora in volo. Due regole: (1) per uno stato **negativo** ("non hai un piano", "nessun risultato", "non sei abbonato") gattare su `isFetching`, non su `isLoading` — non si annuncia un'assenza mentre si sta ancora chiedendo; (2) non persistere payload vuoti: una cache offline di un "niente" non serve a nessuno e trasforma un ritardo in un'affermazione falsa.
- **[2026-07-20] [B292b]**: **L'autenticazione richiede la rete, quindi non può essere un prerequisito per mostrare dati offline.** Clerk.js si scarica dalla rete: senza connessione non inizializza mai, `isLoaded` resta false per sempre e `window.Clerk.user.id` è `null` proprio nel momento in cui la cache offline serve. Tre conseguenze che ho subìto tutte insieme: (1) il guard di scoping leggeva quel `null` come «un altro utente» e **cancellava** la cache a ogni apertura offline — il guard scritto per proteggere i dati li distruggeva; (2) le query gattate su `authReady` restavano disabilitate, quindi anche con la cache intatta non veniva mostrato nulla; (3) si aspettavano 3s di timeout Clerk a ogni avvio offline, cioè schermo bianco garantito. Regole: distinguere sempre **«il provider dice nessuno»** da **«non ho potuto chiedere al provider»** — sono fatti diversi e solo il primo autorizza a distruggere dati; ricordare l'ultimo user id in locale (non è una credenziale, ogni chiamata API vuole comunque un token vero) per avere identità senza rete; e `enabled` deve gattare **la rete, non il rendering**, perché React Query serve comunque la cache di una query disabilitata.
- **[2026-07-20] [B292 — processo]**: Ho consegnato tre volte "provalo" su una feature **offline** verificata solo da test unitari e build verde, e per due volte su tre il codice non era nemmeno deployato (fix su branch, utente che testava `main`). Le tre cause reali — precache di route autenticate che prendeva 404, cache che si autocancellava perché Clerk non carica offline, blocco onboarding che si accendeva su un errore di fetch — **nessuna** era osservabile dai test: erano tutte nell'interazione fra middleware, service worker, provider di auth e rete. Regole: (1) una feature che dipende da rete/auth/SW non è verificata finché non gira **end-to-end nell'ambiente vero** — i test unitari qui dimostrano solo che le singole parti non contraddicono le mie assunzioni; (2) prima di dire "provalo", **controllare che il codice sia dove l'utente andrà a guardarlo** (`git log main --grep`); (3) quando l'utente riporta un guasto, la prima domanda è *quale build sta usando*, non *quale sia il bug*; (4) su un guasto in produzione, curl-are gli URL reali costa un minuto e mi avrebbe dato subito i 404 su `/offline` e `/manifest.json` che ho scoperto solo al secondo giro.
- **[2026-07-20] [A245 Phase G]**: Prima di deduplicare, **misurare** quanto le copie siano davvero identiche invece di fidarsi dell'audit. Dei 9 handler duplicati today↔week, 6 erano identici a meno di spazi e 3 no: le differenze erano formattazione **tranne una vera** (`/week` faceva `setError(null)` prima di riprovare, `/today` no, quindi lì un banner d'errore vecchio sopravviveva al retry). Se avessi copiato una delle due versioni a caso avrei consolidato il comportamento peggiore in un solo posto, rendendolo permanente. E la fotografia da fare prima dell'estrazione non è uno snapshot di rendering: gli **eventi sono valori**, quindi estrarre i costruttori di payload come funzioni pure li rende fissabili con `toEqual` senza renderizzare nulla.
- **[2026-07-20] [A245 Phase G]**: **Un tipo che mente è peggio di nessun tipo.** `ResolvedSession` dichiarava un `blocks` di primo livello che nel payload non esiste (è annidato sotto `resolved_session`, con `block_uid`/`selected_exercises`): l'unico consumer faceva `resolved.blocks.map(...)`, TypeError garantito, mai scattato solo perché a quella route non punta nessun link. Il danno vero non era la pagina morta ma i 220 cast `as X` altrove, che sembravano sicuri appoggiandosi a tipi come quello. Appena reso onesto, il compilatore ha trovato **subito** un secondo problema reale (un campo obbligatorio che il payload non garantisce). Corollario: i test possono confrontare i nomi dei campi TS col sorgente Python che li produce — così un rename lato backend rompe un test invece di renderizzare `undefined` in palestra.
- **[2026-07-21] [A247]**: `hover:` **non esiste su touchscreen**. Il `Button` base dell'app aveva solo stili hover, quindi su telefono premere un bottone non produceva alcun feedback visivo finché non cambiava lo stato — e in una guided session, con il telefono per terra e le mani sporche di magnesite, "ho premuto?" diventa la domanda dominante. Serve sempre uno stato `active:` (scatta al touch-down), e l'aptico via `navigator.vibrate` va trattato come **extra**, mai come unico segnale: su iOS Safari è un no-op, perché Apple non espone il taptic engine al web. Corollario architetturale: un solo punto di emissione (il componente base) invece di chiamate sparse nelle pagine, altrimenti due livelli reagiscono allo stesso tap e si sente un doppio buzz.
- **[2026-07-21] [A247]**: Cambiare il contenuto di una schermata **senza toccare lo scroll** è un bug di percezione, non di stato. Nel guided, premere Done in fondo a una card lunga avanzava correttamente l'esercizio ma lasciava la vista in fondo, davanti a un altro bottone Done: lo stato era giusto, l'utente non aveva modo di saperlo. E "riportare la vista su" non è `scrollTo(0)`: field test sul telefono ha mostrato che header sticky + banner spingevano il titolo del nuovo esercizio sotto la piega — l'ancora giusta è **il titolo del nuovo contenuto**, con offset pari all'altezza dell'header sticky (misurata via ref, non hardcodata) e un `requestAnimationFrame` per aspettare che il nuovo layout esista prima di misurarlo.
- **[2026-07-21] [A247-bis]**: Su iOS Safari/PWA `navigator.vibrate` non esiste, ma da iOS 17.4 c'è un varco: `label.click()` programmatico su un `<input type="checkbox" switch>` nascosto genera il tick aptico nativo. Vincoli non ovvi: l'elemento **non** può essere `display:none` (iOS ignora i toggle non renderizzati — serve il pattern visually-hidden: fixed 1px + clip-path), e il tick va sparato su `pointerdown`, non su `click`, altrimenti arriva dopo il lift-off e si legge come lag. Un solo elemento singleton per tutta l'app, creato lazy al primo tap.
- **[2026-07-21] [B293]**: Una regressione può nascere dall'**interazione di tre pezzi tutti corretti**: B285 (401 anonimo, giusto), il redirect F51 su 401 (giusto), e un provider che chiama `getState()` anche su una pagina pubblica (era innocuo prima di B285). Nessun singolo diff era sbagliato, ma insieme hanno reso irraggiungibile la landing pubblica il giorno dopo. Quando si fa fail-closed su un'API (200→401), grep-are **tutti i caller** e chiedersi: qualcuno chiama da un contesto legittimamente anonimo?
- **[2026-07-21] [B293]**: Leggere `window.Clerk` in un effect di mount è una **race garantita**: clerk-js si carica async e al primo render non c'è quasi mai. Peggio, qui la chiave di storage cambiava a seconda di CHI vinceva la race (`_anon` vs `_user_XXX`), e il fallback "carico dallo stato e salvo" scriveva i default **sopra** il draft buono — perdita dati silenziosa da una riga che sembrava una cache-warm. Identità nel rendering solo via `useAuth()`/`useUser()` (che aspettano `isLoaded`); `window.Clerk` va bene solo in handler post-gesto.
- **[2026-07-21] [B293]**: La validazione per-step di un wizard protegge solo **il passaggio da quello step**: se il draft può cambiare dopo (restore, re-auth, migrazione), la summary e il server rivedono dati mai più validati. Ogni gate va replicato nel punto di submit (client) E nel punto di scrittura (server) — qui "0 kg" è passato dalla review fino a `bodyweight_kg` e da lì in tre moduli engine con tre comportamenti diversi (`or 70.0` silenzioso, 0.0 propagato, `is None` che 0 supera).
- **[2026-07-21] [A248]**: Mai attivare il 308 redirect sul vecchio dominio di produzione **prima** che il DNS del nuovo dominio sia verificato live: per ~5 min il vecchio URL ha rediretto verso un host che non risolveva (prod di fatto giù per chi aveva il vecchio link). Ordine giusto: DNS nuovo dominio → verifica `curl -I` 200 → solo allora redirect sul vecchio. Nota tecnica: il CLI Vercel non espone la config redirect — si fa con `PATCH /v9/projects/<p>/domains/<d>` usando il token da `~/Library/Application Support/com.vercel.cli/auth.json`.
- **[2026-07-21] [A248-bis]**: Un curl 200 NON valida una landing page: i redirect Clerk/Next sono client-side (`router.replace` dopo `useAuth()`), invisibili all'HTTP. La root `/` rispondeva 200 ma mandava gli anonimi su `/sign-in` — i link reddit in convenzione UTM erano quindi sbagliati. Per validare una landing: leggere `page.tsx` + `public-routes.ts` E verificare che l'HTML servito contenga il contenuto atteso, non solo lo status code.
- **[2026-07-21] [A249]**: Clerk BAPI `POST /v1/sessions` è dev-only: su un'istanza production si testa un login reale con `POST /v1/sign_in_tokens` (BAPI) + `POST <frontend-api>/v1/client/sign_ins?_is_native=true&strategy=ticket` (FAPI) → JWT in `client.sessions[0].last_active_token.jwt`.
- **[2026-07-21] [A249-bis]**: `vercel env rm NAME production` rimuove la variabile da TUTTI i target (non solo production) quando è un'unica entry multi-target — dopo il rm, ricontrollare `env ls preview` e ri-aggiungere i target persi. E in una migrazione d'identità l'ordine giusto è: prima chiudere la porta vecchia (swap JWKS → i token vecchi diventano 401), POI rimappare gli id — mai il contrario, o i token vecchi ancora validi creano account orfani.
- **[2026-07-21] [B294]**: Clerk non esporta MAI gli hash password: una migrazione di istanza lascia gli utenti password con `password_enabled=false` e unico factor `reset_password_email_code`, che `<SignIn/>` non renderizza da solo (il link "Forgot password" vive nello step password, mai raggiunto) → utente bloccato dopo "Continue" senza che parta alcuna email. E il check pwned di Clerk vale anche al sign-in: forzare una password compromessa con `skip_password_checks` produce un account che non può comunque loggarsi.
- **[2026-07-23] [A256 / diagnosi login]**: Prima di ri-diagnosticare un sintomo in produzione, **cercare l'ID del fenomeno in `ROADMAP_CURRENT.md` e `lessons.md`**: il "login rotto" del 23/07 era già stato censito e chiuso il 21/07 ([[B294]] + [[B295]]), e ripartire da zero mi ha fatto contare come "utenti bloccati" 8 persone che in realtà erano registrate via **Google** — in prod risultano `password_enabled=false` con `external_accounts=[]` (la migrazione non trasferisce i collegamenti OAuth) ma rientrano al primo tap perché Clerk ri-collega l'account sull'email verificata. `password_enabled=false` NON significa "bloccato": va incrociato con il metodo di registrazione sull'istanza **di origine**, non solo su quella nuova.
- **[2026-07-23] [A256]**: Le preview Vercel sono protette dall'SSO del team, quindi `curl` su una preview URL non verifica nulla (segue il redirect e restituisce 200 della pagina di login Vercel). Per validare regole di **middleware** — quali route sono pubbliche e quali no — serve il dev server locale, e in un `git worktree` va copiato `frontend/.env.local` **aggiungendo `CLERK_SECRET_KEY`** (il file di lavoro ha solo la publishable key: senza secret ogni route risponde 500 e il gating sembra rotto ovunque).
- **[2026-07-28] [B305]**: Mai citare **testualmente** in un system prompt il formato di un messaggio generato dal sistema ("I built you a session — …"): l'LLM lo imita e fabbrica conferme di azioni mai avvenute — in prod il coach ha "confermato" una sessione che il composer non aveva mai costruito (il turno non era nemmeno passato dal composer: il gate regex frontend non instrada follow-up brevi tipo "Si"). Descrivere il comportamento del sistema senza fornire il template imitabile, e vietare esplicitamente al modello di produrre conferme che spettano al sistema.
- **[2026-07-28] [B305]**: Un TODO che dichiara una divergenza "inerte" è una bomba a orologeria: `_exercise_fits_equipment` ignorava `equipment_required_any` e il TODO lo giustificava col filtro role del body-part picker — poi l'adhoc builder (nuovo consumer) ha pescato role=main e i board-only sono finiti nelle sessioni home. Quando si aggiunge un consumer a un helper "light", ricontrollare le assunzioni documentate nei suoi TODO; e la parità va verificata anche altrove (il path prehab-injection di resolve_session ha la stessa divergenza any, tracciata in C-EQUIPMENT-DECLARATIONS).
- **[2026-07-28] [B305]**: La verifica end-to-end con lo **stato prod reale + chiamata LLM reale** trova classi di bug che i test unitari con fixture non vedono: nella stessa run sono emersi ab_wheel/back_extension con `equipment_required=[]`, lo slot `language` che l'extractor non emetteva (serviva `required` nel tool schema) e "60 minuti" che componeva ~30 min. I fixture testano le assunzioni; lo stato reale testa il catalogo.
- **[2026-07-28] [B306]**: Un modello più forte può **regredire** un'estrazione strutturata: sonnet-5 mappava "bloccaggi monobraccio" su `body_parts=[core, back_pulling, biceps]` (anatomia plausibile!) invece che su `focus=pull`, e il path muscle-level — più specifico, quindi prioritario — componeva bicep curl al posto dei lock-off. Sonnet-4-6 non lo faceva perché estraeva *meno*. Quando due slot di uno schema competono con una regola di precedenza ("body_parts batte focus"), il prompt deve dire esplicitamente cosa NON è quello slot: i nomi di **movimento** (bloccaggi, trazioni, sospensioni) non sono parti del corpo. Corollario: dopo uno swap di modello, ri-verificare le estrazioni strutturate, non solo la qualità della prosa.
- **[2026-07-28] [B306]**: Comporre in silenzio meno di quanto chiesto è peggio che rifiutare. Il builder adhoc aveva la nota di onestà solo sul path muscle-level (A252): sul path coarse-focus un pool vuoto ("technique a casa") produceva un guscio warmup+finisher senza dire nulla, e un budget di 60 minuti ne componeva ~30 perché il cap sul numero di esercizi mordeva prima del tempo. Un generatore vincolato deve sempre dichiarare quale vincolo ha morso — o riempire il budget (scalando il volume) o dirlo.
- **[2026-07-28] [C260-bis]**: Un test che asserisce «X vince la selezione» quando il suo nome e il suo commento dicono «X è eleggibile» si rompe al primo cambio legittimo di ranking, e la rottura *sembra* una regressione. Qui correggere `cable_woodchop` (dichiarava `weight`, serve un cavo) lo ha reso eleggibile in una location con soli cavi, dove ha superato `pallof_press`: comportamento corretto, test rosso. Quando un test fallisce dopo un fix di dati, prima chiedersi *quale invariante intendeva davvero proteggere* — spesso è più debole di quella scritta, e va asserita direttamente (isolando il candidato) invece di passare per una selezione competitiva.
- **[2026-07-29] [D261]**: Quando un ranking ha più criteri, verificare che i dati li **alimentino** davvero: `adhoc_builder._rank_key` ordinava per affinità di fase → non-recente → id, ma `phase_affinity` esiste su 1 esercizio su 255 e il termine "non-recente" è morto (i recenti sono già hard-esclusi a monte), quindi la selezione era **puramente alfabetica** — in ogni sessione, per ogni focus. Si vedeva a occhio nudo negli output (`archer`, `band_assisted`, `barbell_row`; `ab_wheel`, `back_extension`, `cable_woodchop`) e nessuno l'aveva letto come alfabeto. Corollario: un campo che il codice legge ma il catalogo non popola è una feature che *sembra* esistere — la roadmap di A243 dichiarava il builder "phase-aware" da mesi.
- **[2026-07-29] [D261]**: Il fix "ovvio" al sintomo può rompere un consumer che il sintomo non nomina. Splittare il pattern `pull_vertical` per far emergere i bloccaggi avrebbe rotto il blocco `lock_off_hold` di `pulling_strength_compound`, che filtra proprio quel pattern per selezionare *quel* bloccaggio: il piano sarebbe collassato in tre trazioni per curare una sessione adhoc. Prima di cambiare un valore di catalogo usato come **chiave di filtro**, elencare tutti i blocchi che ci filtrano sopra — non solo il modulo in cui si è visto il bug.
- **[2026-07-29] [B307]**: Un cap di diversità va applicato sull'asse che esprime «stessa cosa» per il **dominio**, non sul primo campo che sembra adatto. `pattern` raggruppa 12 esercizi su 14 sotto `pull_vertical`, quindi il cap scartava un bloccaggio isometrico come se fosse una quinta trazione; `recency_group` — già presente e già usato da B159b — distingue lock-off, monobraccio ed eccentriche. Prima di introdurre una tassonomia nuova, controllare se il catalogo ne ha già una che dice la cosa giusta (e normalizzarne i sinonimi, o il cap si aggira da solo).
- **[2026-07-29] [A257]**: Per popolare un campo di dominio su 255 righe, cercare prima se la metodologia è **già codificata altrove nel repo**: `_BASE_WEIGHTS` (quale asse conta in quale fase) e `PHASE_INTENSITY_CAP` (quanto si spinge) erano in `macrocycle_v1` da sempre, e incrociarli ha prodotto un'assegnazione difendibile e verificabile a mano, invece di 255 giudizi a intuito. Derivare una volta e **committare i dati** (con doc + test di coerenza) invece di lasciare uno script di rigenerazione: lo script diventerebbe una seconda fonte di verità da mantenere allineata al catalogo.
- **[2026-07-29] [A257]**: Una regola derivata va **ispezionata sui casi limite prima di scriverla**, non dopo. La media sui pesi funzionava per gli assi con un picco, ma su `core_prehab` — piatto per design (0.10 ovunque) — sceglieva l'unica fase leggermente più alta e lasciava 34 esercizi senza affinità; e sul deload l'asse non seleziona nulla, perché uno scarico non enfatizza un asse ma chiede lavoro leggero. Due eccezioni esplicite (asse piatto ⇒ tutte le fasi; deload ⇒ solo intensità) hanno portato la copertura da 221/255 a 254/255. Il dry-run con distribuzione + campione di controllo costa un minuto e ha trovato entrambe.
- **[2026-07-29] [A257]**: Tre test sono caduti non perché il cambiamento fosse sbagliato ma perché **asserivano su quale esercizio vincesse una selezione competitiva** mentre il loro nome dichiarava altro (filtro equipment, plumbing del carico). È lo stesso schema del test A229 caduto il giorno prima: un test che passa attraverso un ranking per verificare un'invariante che col ranking non c'entra è fragile per costruzione. Rimedio: isolare il soggetto (una fixture col solo candidato, o uno scenario dove l'invariante è visibile) — così il test si rafforza invece di essere piegato.
- **[2026-07-29] [D262]**: Una simulazione che alimenta uno stato utente **finto** va validata contro la funzione che lo legge, prima di trarne conclusioni. Per misurare la copertura del catalogo avevo costruito `recent_sessions[].exercise_ids`, mentre `_recent_exercise_ids` legge `recent_sessions[].exercises[].exercise_id`: la recenza risultava sempre vuota, niente rotazione, e la misura diceva «8 esercizi su 38 raggiungibili» — cioè 30 falsi morti. Il campanello è stato il numero **esattamente uguale a `MAX_EXERCISES`**: una copertura che coincide con un limite di sistema non è una misura, è il limite che si sta guardando. Con la forma giusta: 26/27. Un `assert` di due righe sulla forma dello stato prima del loop avrebbe evitato tutto.
- **[2026-07-29] [D262]**: Per dimostrare che un elemento di catalogo è irraggiungibile bisogna modellare **tutti** i percorsi di selezione, non solo quelli dichiarati come filtro: oltre ai 149 filtri di sessioni/template esistono l'iniezione prehab guidata dalle limitazioni utente e il flusso dei test, che nessun blocco esprime. Senza quei due, 18 esercizi risultavano irraggiungibili; con loro, 16 — e nessuno davvero morto. Un audit di raggiungibilità va costruito enumerando i **consumer del dato**, non i file che sembrano contenerlo.
- **[2026-07-29] [D262]**: Un difetto si vede meglio nell'output di una feature nuova che nei test della feature che l'ha introdotto. Il riempimento di budget di B305 aveva superato tutta la sua suite, ma alla prima sessione handstand ha prodotto `2×600s` di pratica libera — 20 minuti su 59 per un solo drill — perché il numero di serie non è un volume per un blocco prescritto come "pratica per N minuti". Guardare l'output reale di un caso d'uso nuovo è un test che nessuna asserzione esistente sostituisce.
- **[2026-07-29] [D263]**: Terza volta in due giorni che una simulazione mi mente perché lo stato finto non ha la forma che il codice legge — e stavolta in tre modi diversi nella stessa indagine: `resolve_session` prende la location da `user_state["context"]["location"]` (senza, tutto viene filtrato via con `after_location: 0`), la recenza da `week_plans` via `load_recent_exercise_ids` e non da `recent_sessions`, e il seed di varietà settimanale (B274) da `context["date"]` (senza data il tie-break torna alfabetico e un solo esercizio vince sempre). Ogni omissione produceva un risultato **plausibile e sbagliato**: "il blocco non si risolve", "non ruota mai", "vince sempre archer". Regola: prima di simulare, leggere la funzione che consuma lo stato e replicarne la forma **campo per campo**; se un risultato sembra troppo netto (uno stesso vincitore 10 volte su 10) è quasi sempre un input muto, non un bug del sistema.
- **[2026-07-29] [D263]**: Un catalogo può contenere una sessione perfetta per un bisogno e non servirla mai. `pulling_strength_gym` esiste con tre blocchi dedicati a trazione pesante, bloccaggio e typewriter, ma non è nel pool di nessuna fase: il file c'è, i template ci sono, i test passano, e l'utente non la vede mai. Quando si verifica la copertura di un catalogo bisogna controllare anche l'anello **sessione → pool di fase**, non solo esercizio → blocco: un orfano a quel livello è invisibile a qualunque audit che parta dagli esercizi.
- **[2026-07-29] [C261 / incidente B308-B309]**: Una regola di isolamento **condizionale** non isola. «Usa un worktree *se* il tree è sporco» ha fallito per la terza volta perché il preflight è una **fotografia istantanea**: due sessioni trovano il tree pulito, restano entrambe nell'albero condiviso, e quaranta minuti dopo una fa `git checkout -b` spostando HEAD anche all'altra, il cui commit finisce sul branch sbagliato. Le prime due volte la reazione era stata scrivere `scripts/start_brief.py`; il tool esisteva ed era corretto, ma nessuno lo obbligava. Un invariante che dipende da chi si ricorda di rispettarlo non è un invariante: serve un guard che fallisca da solo (qui un `pre-commit` che blocca i branch non-main nel worktree primario) e una regola incondizionata.
- **[2026-07-29] [B308]**: Quando serve un dato derivabile dal catalogo dentro un modulo caldo, guardare **prima** se il progetto ha già deciso come farlo. `planner_v2` non poteva leggere le sessioni per sapere quali allenano la tirata: A245 E-6 aveva tolto proprio quell'I/O a import-time perché costava a ogni boot e falliva in silenzio. La soluzione di casa è dichiarare il flag in `_SESSION_META` e mettere un test che lo confronta col catalogo — il metadato mente solo finché la suite non gira.
- **[2026-07-29] [B308]**: Un criterio di appartenenza troppo largo produce categorie assurde senza segnalare nulla. Definire "sessione che allena la tirata" via il dominio `strength_general` faceva rientrare `legs_strength` e `lower_body_gym`, perché quel dominio copre 65 esercizi fra cui squat e panca. Il criterio giusto guarda il **pattern** (`pull_vertical`/`pull_horizontal`) o i domini specifici. Se una classificazione automatica restituisce membri che a leggerli fanno ridere, il problema è il criterio, non i dati.
- **[2026-07-29] [B308]**: Far **dichiarare a un sistema quando non ce l'ha fatta** vale più della funzione che implementa. Il `PASS 2.6` non è riuscito a collocare la tirata per un utente solo-casa, e proprio quel `unmet_stimulus` ha rivelato un buco di catalogo che nessuno aveva visto: delle 6 sessioni che allenano la tirata una sola è eseguibile senza palestra. Un fallimento silenzioso avrebbe lasciato quell'utente senza tirata e senza traccia — che è esattamente come D263 è rimasto invisibile per mesi.
- **[2026-07-29] [B309]**: Il worktree condiviso non fa danni solo con `git checkout`: con due sessioni attive, il `git add -A` dell'altra ha inglobato la mia voce di roadmap non ancora mergiata e l'ha pushata su `main`, così al merge del branch la stessa riga risultava aggiunta da entrambi i lati (conflitto) — e il primo segnale è stato un innocuo "the file had been modified on disk" del tool Edit. Due regole: (1) trattare quel messaggio come "un'altra sessione sta scrivendo qui", non come rumore, e rifare `git log`/`git status` prima di stagiare; (2) risolvere quel tipo di conflitto **per hunk tenendo il lato `main`** (che contiene già il lavoro dell'altro brief) e conservando solo le proprie righe nuove — `--ours`/`--theirs` sul file intero perde sempre qualcosa.
- **[2026-07-29] [A258]**: "Eleggibile" e "collocato" sono due cose diverse, e solo la misura le distingue. Mettere la sessione dedicata nel pool come `available` — che sulla carta rispettava il "in sostituzione, non in aggiunta" del KB — non la faceva collocare **mai**: perdeva contro le primarie e la settimana si riempiva prima. Da `primary` entra al posto di un'altra seduta dura, coi giorni duri invariati, che è ciò che il KB voleva davvero. Quando si aggiunge un elemento a una struttura di scelta competitiva, verificare che venga *scelto*, non solo che sia presente.
- **[2026-07-29] [A258]**: Un vincolo a valle può annullare in silenzio una decisione presa a monte. Il KB prescriveva la sessione di forza di tirata anche in `base`, ma `base` ha `PHASE_INTENSITY_CAP = medium` e la sessione è `high`: il planner la scarta prima ancora di considerarla, quindi dichiararla nel pool sarebbe stata una promessa senza effetto. Prima di implementare una raccomandazione, controllare che non esista un filtro più a valle che la rende inerte — e se esiste, dirlo invece di scrivere codice che sembra funzionare.
- **[2026-07-29] [A258]**: Se un dato influenza la generazione, deve **viaggiare col risultato**, non essere ricostruito da chi lo consuma. Il replanner rifabbricava il session pool da `(phase_id, discipline)`: bastava che il pool dipendesse da qualcos'altro — la disciplina in B287/R-3, il profilo qui — perché una settimana replanificata divergesse da quella generata. La cura non è passare più parametri al ricostruttore, è smettere di ricostruire: il pool ora sta nello `profile_snapshot` accanto ai `domain_weights`, che erano lì da sempre per la stessa ragione.
- **[2026-07-29] [C262]**: Prima di "chiudere un rischio latente" con un guard difensivo, leggere **perché** il guard non c'è già. `B-SUB-TRIAL-RECONCILE` proponeva di scadere le righe `trialing` con `trial_end` passato; il codice le esclude di proposito e il commento lo dice: un webhook Stripe in ritardo bloccherebbe fuori un utente che ha appena convertito. Il guard avrebbe scambiato un leak raro (zero casi attivi, misurato) con il fallimento peggiore per un prodotto a pagamento. Quando il rimedio è più costoso del male in un ramo, la risposta giusta è **osservare** (l'alert c'è già in `gtm_funnel.py`), non bloccare.
- **[2026-07-29] [C262]**: Un timore che blocca un fix va **misurato**, non ereditato. Gli estensori dita non dichiaravano l'elastico da mesi per paura di togliere il prehab a chi non ne ha: bastava contare i candidati rimasti per scoprire che l'alternativa senza attrezzo esisteva già in entrambi i domini (`finger_tendon_glides`, `forearm_stretches`). Il fix era sicuro dal primo giorno; a mancare era la verifica, non la soluzione.
- **[2026-07-30] [B312]**: Un fix può essere corretto nella formula e **inerte nella lettura**. D151 aveva ricalibrato `session_load_score` e i suoi test passavano, ma i due consumatori del report lo cercavano su `slot["session_load_score"]` mentre `resolve_session` lo scrive sul payload risolto (`slot["resolved"]`): per mesi ogni sessione è caduta in silenzio sul fallback `estimated_load_score`. I test lo iniettavano a mano sul livello sbagliato, quindi confermavano la lettura invece di verificarla. Quando un campo viaggia fra moduli, il test deve costruire il dato **come lo costruisce la produzione** (qui: risolvere davvero, o replicare l'annidamento) — altrimenti verifica la propria fixture.
- **[2026-07-30] [B312]**: `or` come fallback cancella lo zero legittimo. `s.get("x") or s.get("y")` era innocuo finché `x` era solo un prescritto (zero = "il resolver non ha prodotto nulla" → fallback giusto), ma è diventato un bug appena è arrivato un valore *misurato* dove zero è una risposta vera (l'utente ha skippato tutto). Prima di aggiungere una chiave a una catena di fallback, chiedersi se il suo zero significa "assente" o "misurato zero": nel secondo caso serve `is not None`, per quella chiave e non per le altre.
- **[2026-07-30] [B313]**: Se un'anteprima e l'azione che ne consegue leggono **sorgenti diverse**, prima o poi divergono. A210 mostrava l'override boulder da uno `useState` della card, mentre `handleStartGuided` — funzione module-level, senza accesso a quello stato — costruiva la sessione dal piano: la preview diceva boulder, il player faceva lead. La domanda da farsi quando si aggiunge uno stato locale che modifica ciò che l'utente vede: *chi altro legge questo dato quando l'utente agisce?* Se la risposta è "un'altra funzione, da un'altra fonte", lo stato locale è il posto sbagliato.
- **[2026-07-30] [B313]**: "Effimero" può significare due cose molto diverse — *non modifica il piano* oppure *non ha effetto*. A210 fu progettata effimera nel primo senso e implementata nel secondo. Quando una decisione di design dice "senza persistenza", chiedersi fin dove deve arrivare l'effetto: qui bastava scriverlo sullo slot della giornata (già coperto da `_user_edited`) per accontentare entrambi i sensi.
- **[2026-07-30] [B313]**: Sostituire una sessione con la sua "variante boulder" dichiarata a catalogo non produce bouldering. Con entrambe le pareti disponibili, `boulder_circuit_gym` seleziona `route_on_the_minute` — un esercizio da corda — per il blocco primario. Ciò che rende boulder una sessione non è il suo nome ma **l'attrezzatura disponibile al momento della risoluzione**: il `boulder_fallback` sceglie *quale* sessione, togliere `gym_routes` dall'equipment è ciò che mantiene la promessa. E senza parete boulder il resolver ripiega in silenzio su hangboard, quindi la disponibilità va verificata prima, non dedotta dall'output.
- **[2026-07-30] [B314]**: Un messaggio d'errore che **indovina la causa** manda a caccia di bug inesistenti. La card di [[B313]] diceva «is there a boulder wall at this gym?» per qualunque fallimento: il primo tap reale prese un banale 404 (backend non ancora deployato) e la diagnosi apparente fu «l'app non riconosce il Kilter». Costo: un giro di indagine su codice corretto. Quando il server sa già perché ha rifiutato, mostrare il suo `detail`; e riservare un messaggio esplicito al 404, che in una PWA significa quasi sempre client più nuovo del backend.
- **[2026-07-30] [B314]**: Su Vercel + Railway le due metà **non atterrano insieme**: Vercel pubblica in ~1 minuto, Railway può non partire affatto (qui l'auto-deploy da GitHub non è scattato per due push di fila, ultima build ferma a otto ore prima). Finché il backend non ha l'endpoint, il frontend nuovo lo chiama e prende 404 — quindi dopo un merge che tocca entrambi, **verificare il backend prima di dire "è deployato"**: `curl /openapi.json | grep <endpoint>` è la prova, non il tempo trascorso.
- **[2026-07-31] [A259]**: Un vincolo **negativo** non si esprime con la stessa tassonomia dei vincoli positivi. "Niente trazioni" sembra `pattern == pull_vertical`, ma un bloccaggio È una trazione verticale: quel filtro avrebbe cancellato esattamente ciò che l'utente chiedeva. E `recency_group == pullup_variants`, più fine, lascia passare `frenchies` — che il catalogo raggruppa coi bloccaggi perché è **lo stimolo che allena**, mentre ogni ripetizione apre con una trazione completa. Le tassonomie di un catalogo sono costruite per un asse (stimolo, recupero, pattern); un rifiuto dell'utente vive su un asse diverso e va mappato a mano, con un test che obbliga a classificare ogni nuovo membro.
- **[2026-07-31] [A259]**: I limiti tarati per un algoritmo cieco strozzano quello informato. `MAX_EXERCISES=8` è giusto per un builder che pesca a ranking (oltre sarebbe padding) e sbagliato per una sessione composta su richiesta esplicita di cinque blocchi: tagliava la coda — le flessioni — **dopo** che la spiegazione le aveva promesse. Quando si aggiunge un secondo produttore a valle della stessa struttura dati, i suoi limiti vanno riderivati dal suo scopo, non ereditati.
- **[2026-07-31] [A259]**: Un modello che riempie 31 minuti su 60 non si corregge spiegandogli meglio come contare: si corregge **misurando** e rimandandogli il numero. Un solo giro correttivo con la durata calcolata dal motore («hai riempito 31 dei 60 minuti, questi sono gli esercizi, rifallo») ha portato la stessa richiesta a 50-63 minuti. Il feedback quantitativo di ritorno vale più di qualunque istruzione a priori nel prompt.
- **[2026-07-31] [A259]**: I test dal vivo hanno trovato ciò che 22 unit test non potevano: `frenchies` in una sessione senza trazioni, le flessioni promesse e non consegnate, quattro protocolli massimali in un'ora. Quando si dà a un LLM un compito generativo, l'unica verifica che conta è **eseguirlo sui casi reali dell'utente** e leggere l'output come lo leggerebbe lui — la suite protegge dalle regressioni, non dalla stupidità plausibile.
- **[2026-07-30] [B310/D265]**: Una diagnosi plausibile può essere falsa anche quando i sintomi tornano: avevo concluso "il router del KB è monolingua inglese" dai fallback, ma l'indice conteneva già le keyword italiane — il difetto vero era il match esatto su lemmi (`dormire`) contro le forme flesse che l'utente digita (`dormo`). Leggere i dati prima di dedurre dalla forma dell'errore, e correggere la diagnosi *nel documento dove è scritta*, non solo nel commit che la supera.
- **[2026-07-31] [D265→D267]**: Una suite di regressione LLM misura l'infrastruttura almeno quanto il contenuto. Il punteggio del coach è salito da 43/56 a 52/56 senza toccare una riga del knowledge base: i punti persi venivano dal router che non caricava il file giusto, da un KB che dichiarava un valore che il motore non aveva, e da un `max_tokens` tarato sull'inglese che tagliava a metà una risposta italiana su tre. Quando uno scoring va male, sospettare prima la pipeline che il modello.
- **[2026-08-01] [B316/B317]**: iOS 26 congela `CPU iPhone OS` a `18_6` nell'UA — per la versione usare `Version/nn`; e le costanti UA nei test vanno copiate da device reali, non inventate (il test era verde su `OS 26_0`, stringa che nessun iPhone invia).
- **[2026-08-01] [B318]**: un elemento SVG può tagliare il proprio contenuto senza che il viewport c'entri — se il testo esce dal `viewBox`, è tagliato a ogni larghezza; misurare la geometria in una funzione pura invece di fidarsi dell'occhio su laptop.
- **[2026-08-01] [B319]**: due percorsi per gli stessi dati (payload API e draft del wizard) divergono appena se ne aggiorna uno solo — quando si aggiungono campi a un form, cercare tutti i consumatori dello stato, non solo quello che si sta toccando.
- **[2026-08-01] [C265]**: un test parametrizzato su file scoperti dal filesystem cambia il numero di casi collezionati fra macchine (`_archive/` è gitignored) — e quel numero `sync_status.py` lo scrive nei doc e il pre-push hook lo confronta. Se la parametrizzazione non aggiunge diagnostica che il messaggio d'errore non abbia già, una passata unica è più corretta.
- **[2026-08-01] [C265]**: una segnalazione può avere ragione sul sintomo e sbagliare il punto — il testo "fuso" del kick-up non esisteva nel repo: era una entry coerente ma italiana, letta accanto al drill vicino. Il difetto vero era l'ambiguità («fermandosi al muro» non diceva quale lato), cioè proprio dove i due esercizi divergono. Cercare la stringa esatta prima di accettare la diagnosi.
- **[2026-08-02] [D269]**: un item di roadmap marcato Open non è una prova che il lavoro manchi — B176 stava «Open, effort L, 21 finding» con quattro commit `fix(B176)` in git: prima di stimare un brief, `git log --all --grep=<ID>`.
- **[2026-08-02] [D269]**: un finding tracciato per ID vive quanto il documento che lo definisce — D163 e D172 erano citati da anni con puntatori a file mai creati o cancellati «archiviandoli», e i loro residui oggi non sono più azionabili.
- **[2026-08-02] [D269]**: quando si smonta una feature, le sue promesse restano accese altrove — il recovery code è stato tolto da Settings e dal client ma è rimasto nella CTA della landing, nella guida utente e in due endpoint vivi: cercare la feature per nome in *tutte* le superfici, non solo nel modulo che si sta togliendo.
- **[2026-08-02] [B320]**: la guida utente esiste in **due** copie — `docs/user_guide_v1.md` e `frontend/src/lib/guide-content.tsx`, che è quella che l'utente legge davvero dentro l'app: correggerne una sola lascia in piedi la versione che conta.
- **[2026-08-02] [B320]**: quando si ritira una feature, il cleanup dei dati che la riguardano non va con lei — `delete_user_data()` deve continuare a cancellare la tabella, altrimenti eliminare un account lascia righe orfane.
- **[2026-08-04] [coaching prod]**: un giorno outdoor nel week plan non vive in `day["sessions"]` ma su campi a livello di giorno (`outdoor_spot_name`, `outdoor_discipline`, `outdoor_session_status`) — un dump che itera solo le sessioni lo mostra come REST e lo fa sembrare libero.
- **[2026-08-05] [B321]**: un grado fuori tabella che diventa `index 0` non è un fallback, è un dato falso — `else 0` su una lookup di gradi va sempre letto come bug (il neutro è 50, non lo zero).
- **[2026-08-06] [B322]**: un default di form diventa un vincolo del motore appena viene salvato — `preferred_location: "home"` preselezionato dal wizard era indistinguibile da una scelta esplicita, e il planner lo trattava come tale cancellando l'intera settimana in palestra: se un campo è vincolante a valle, il default deve dipendere da ciò che l'utente può davvero fare.
- **[2026-08-06] [B322]**: `[]` e `None` non sono la stessa assenza — «kit di casa dichiarato vuoto» e «kit sconosciuto» devono divergere, ed è esattamente la distinzione che permette di correggere un vicolo cieco senza rompere la preferenza di chi a casa si allena davvero.
- **[2026-08-06] [B322]**: quando un test rosso difende una promessa fatta all'utente, è il fix a essere sbagliato — 11 test hanno intercettato una prima stesura che trasformava «martedì mi alleno a casa» in una sessione in palestra.
- **[2026-08-06] [A265]**: `git checkout <branch> -- backend/` per portare una parte di brief su main riporta indietro **tutto** ciò che la directory contiene, incluso il lavoro che main ha ricevuto dopo la creazione del branch (qui B322, mergiato da un'altra sessione mentre A265 era in corso) — elencare i file esplicitamente, e leggere sempre `git status` prima di committare.
- **[2026-08-06] [A266]**: «non inventare il coefficiente» non vuol dire «non implementare» — la costante giusta si trova incrociando fonti finché convergono, e se il numero che cade dalla tabella interna sta dentro il range della letteratura indipendente hai una derivazione, non una stima.
- **[2026-08-06] [A266]**: un bound di plausibilità deve filtrare l'impossibile in assoluto, non l'improbabile per quell'utente — scartare un dato perché incoerente con gli altri assi sarebbe inventare una regola, e il posto giusto per quel dubbio è una domanda all'atleta.
- **[2026-08-06] [C268]**: «controlla se serve aggiornare la guida» è stato saltato da tre brief di fila ([[B304]], [[A267]], [[A268]]) perché nessuno *rompeva* la guida — ma cambiavano il significato di un numero che la guida non aveva mai spiegato: quando un brief cambia come si legge un dato, la guida va toccata anche se non contiene ancora nulla da correggere.
- **[2026-08-06] [A267]**: il setup di test non renderizza componenti, quindi una garanzia «solo visualizzazione» non si dimostra con un test di render ma con i confini del modulo — chi importa il modulo, che cosa importa lui, e asserzioni di purezza sull'input e sulla tabella dati; un grep sui consumatori vale come prova solo se è ripetibile e scritto nel report.
- **[2026-08-07] [B326]**: uno stato che scade "pigramente" (`trialing` fino al prossimo colpo sulla guard) non va mai letto verbatim in un report — il DB conserva lo stato vecchio proprio per chi non torna più, cioè esattamente la popolazione che il report deve mostrare come persa.
- **[2026-08-07] [B326]**: una metrica di engagement va verificata contro un utente che *non* ha fatto nulla — «ha un `last_active` su Clerk» era vero per il 100% degli iscritti, quindi non misurava niente; il segnale d'attivazione è la sessione completata, e va sempre con la data dell'ultima (un totale a vita non distingue un utente attivo da uno fermo da quattro mesi).
- **[2026-08-07] [D273]**: per capire cosa ha visto un utente in un certo giorno non basta rigiocare il router — va congelato `datetime.now()` a quel giorno, altrimenti `preserve_before` pianifica attorno ai giorni ormai passati e si guarda una settimana che quell'utente non ha mai visto.
- **[2026-08-07] [D273]**: un piano può essere corretto e inerte allo stesso tempo — rispettare disponibilità, luoghi e attrezzatura dichiarati non basta se la prima cosa che l'utente vede non è una cosa che può fare adesso; l'aderenza al vincolo non è una metrica di prodotto.
- **[2026-08-08] [GTM-05]**: su r/climbharder il registro conta quanto il contenuto — il post è stato letto come «scritto con l'AI», sommerso di commenti negativi e chiuso con un ban in due giorni: il prodotto non è mai stato giudicato, e il canale è bruciato per sempre.
- **[2026-08-08] [GTM-05 / D273]**: un audit che dice «non portare traffico prima di aver chiuso questo salto» va preso alla lettera — D273 l'aveva scritto prima del post, e il traffico è finito su un funnel che perde tutti fra macrociclo generato e prima sessione.
- **[2026-08-09] [B329/B330]**: quando un brief sposta un effetto collaterale lato server (B277 chiude il piano dentro `POST /outdoor/log`), va rimossa la chiamata client che faceva già la stessa cosa — altrimenti restano due percorsi che scrivono la stessa riga, e il duplicato passa inosservato finché non lo si legge a mano mesi dopo.
- **[2026-08-09] [B330]**: `disabled={submitting}` non è una guardia contro il doppio tap — `setState` è asincrono, quindi due click nello stesso batch React entrano entrambi nell'handler; serve un lock su `useRef`, che si chiude in modo sincrono.
- **[2026-08-09] [B327]**: un fattore moltiplicativo basato su un campo che l'utente non compila con cura (la durata outdoor, che esclude i riposi) non aggiunge segnale ma lo distorce — qui premiava chi riposava di più, cioè l'opposto della fisiologia.
- **[2026-08-09] [B328]**: prima di dire a un utente «questo dato non c'è», elencare le tabelle e cercarci dentro — le vie di Berdorf erano in `outdoor_logs`, non nel riassunto `state.outdoor_log` che avevo guardato; lo stesso buco era nel prompt del Coach, che rendeva i gradi senza l'esito e trasformava due cadute su un 8a in un 8a chiuso.
- **[2026-08-16] [B332]**: un timer che decide transizioni di stato confrontando una deadline wall-clock con `Date.now()` deve marcare la deadline come consumata — i tick che cadono prima del commit di React rientrano nello stesso ramo con la closure vecchia, e un `setCurrentSet(s => s + 1)` funzionale incrementa a ogni giro (misurato: `Set 1/4` → `Set 6/4` in un solo risveglio dal background).
- **[2026-08-16] [B332]**: «il riposo è scaduto» non è «la serie è stata fatta» — far avanzare un contatore di lavoro svolto su una scadenza temporale scrive nei dati sessioni che nessuno ha eseguito; l'avanzamento deve venire da un gesto dell'utente, o almeno solo da una scadenza osservata in foreground.
- **[2026-08-17] [B333/D278]**: un tetto su una lista ordinata è una politica di selezione travestita da limite tecnico — `MAX_POOL=120` su un pool ordinato per `id` rendeva invisibili al coach 42 esercizi, sempre la coda dell'alfabeto; è la stessa classe di [[B274]] (drill `tech_*` fuori rotazione), quindi quando si mette un cap va misurato subito se morde e va loggato quando scatta.
- **[2026-08-17] [D279]**: rieseguire il motore sullo stato di produzione vero trova cose che la lettura del codice non trova — il filtro pre-trip *sembra* corretto leggendo `planner_v2` (il gate c'è), ma girando `generate_phase_week` sulla disponibilità reale di Daniele si vede `pass2.6` piazzare una sessione di dita `hard` su un giorno marcato `pretrip_deload`: il gate esisteva solo nel pass 1.
- **[2026-08-17] [B335]**: una componente normalizzata 0–1 che satura a 0 non «penalizza al massimo», **si toglie di mezzo** — sopra i 26 °C il peso del 30 % della temperatura usciva dalla somma e il punteggio saliva col caldo, perché il caldo alza il dew spread e abbassa l'umidità; quando una componente satura, il suo peso va ridistribuito o imposto come tetto, non lasciato evaporare.
- **[2026-08-17] [B335]**: in un test che filtra per data, la costante epoch va verificata dentro il test — una `_D0` sbagliata di una settimana filtrava via ogni step del forecast e faceva passare in verde asserzioni `is None` che non provavano niente.
- **[2026-08-17] [B336]**: «prendi la copia con più elementi» non è una regola di merge sicura per uno stato che l'utente può *ridurre* — la cancellazione di una via è l'unica modifica che rende il locale più corto del server, quindi quella regola resuscita esattamente ciò che l'utente ha appena rimosso; con un solo lato che porta un timestamp, la precedenza incondizionata al lato che scrive per primo è più onesta di un merge inventato.
- **[2026-08-17] [B336]**: quando il percorso di ripiego è più robusto di quello consigliato, il difetto è nel percorso consigliato — «Log without timer» era offline-safe da A245 mentre la sessione col timer non lo era, e nessuno se n'era accorto perché la scorciatoia funzionava.
- **[2026-08-18] [B337]**: verificare un valore calcolato dal server richiede di ricostruire **l'input esatto del server**, campi derivati inclusi — filtrando via `alt_sides` (che `_enrich_exercise_display` copia dal catalogo e che raddoppia le serie) ho concluso «Railway gira ancora il codice vecchio» mentre il deploy era corretto: il numero non tornava per colpa del mio script, non del server.
- **[2026-08-18] [B337]**: una funzione di stima usata anche come **budget** non è cosmetica — `session_composer` e `adhoc_builder` trimmano finché la stima supera i minuti richiesti, quindi sottostimare la durata li faceva riempire troppo: chi chiedeva 30 minuti riceveva una sessione più lunga.
- **[2026-08-18] [B337]**: `x or default` su un campo numerico trasforma uno **0 esplicito** nel default — `rest_between_sets_seconds or 60` fatturava 60s di riposo a chi aveva scritto 0.
- **[2026-08-19] [B339]**: un componente di terze parti che non renderizza **nulla** finché il suo servizio non risponde (`<SignIn />` di Clerk) produce uno schermo nero indistinguibile da un'app rotta — la pagina che lo monta deve possedere il proprio stato di caricamento e di fallimento, altrimenti un blocco di rete lato utente viene letto come nostro difetto.
- **[2026-08-19] [B339]**: un controllo la cui navigazione è eseguita dal JS di un provider (`<SignInButton>`) è **morto e silenzioso** se quello script è bloccato: visibile, cliccabile, senza effetto. Un `<Link href>` arriva comunque a destinazione, dove un errore può almeno spiegarsi.
- **[2026-08-19] [B339]**: prima di diagnosticare «è rotta la produzione», riprodurre con un browser vero (Playwright, anche con Service Worker attivo) — qui il flusso funzionava end-to-end e il sintomo si riproduceva solo bloccando il dominio del provider, il che ha spostato l'indagine dal codice alla rete dell'utente senza tirare a indovinare.
- **[2026-08-21] [A275]**: un test che passa **solo perché la funzione sotto esame scavalca una cache condivisa** non sta verificando il contratto che dichiara — i tre test A225 usavano tutti le stesse coordinate e sono caduti nell'istante in cui la funzione è entrata nella cache: la fixture di isolamento andava messa quando la cache è nata, non quando qualcuno ci inciampa.
- **[2026-08-21] [A275]**: OpenWeatherMap etichetta gli step con `dt_txt` in **UTC** — filtrare o scegliere «lo step di mezzogiorno» su quel campo dà l'ora giusta solo a UTC+0; scrivere le fixture nel fuso di casa (UTC+2) rende il difetto quasi invisibile, a UTC+3 salta fuori subito.
- **[2026-08-21] [A276]**: `monkeypatch.setattr(mod, "f", ...)` non tocca un *default argument* già legato alla definizione (`def g(path=SESSION_FILE)`) — un test così ha scritto un session id finto nel `.claude_bridge_session` vero e rotto il resume del bridge reale: patcha la funzione che scrive, non la costante che credi stia leggendo.
- **[2026-08-21] [A276]**: `claude -p --resume <id>` fallisce se la sessione non esiste più, e uno strumento che salva l'id su file resta bloccato su quell'errore a ogni messaggio finché qualcuno non interviene a mano — se lo strumento serve proprio quando sei lontano dalla macchina, il recupero automatico (butta l'id, ritenta una volta) non è una rifinitura.
- **[2026-08-21] [A276]**: quando gira dentro il bridge Telegram, il processo `claude -p --resume` corrente **è** il figlio del bridge — riavviare il bridge (tmux `C-c` + rilancio) mentre si sta rispondendo manda il padre in shutdown mentre il proprio output è ancora atteso, rischiando di perdere la consegna della risposta. Con codice cambiato nel bridge stesso, riavviarlo va rimandato all'utente o fatto solo a turno concluso, mai a metà della propria esecuzione.
- **[2026-08-21] [A276]**: `whisper-cpp` (`whisper-cli`) non decodifica ogg/opus nativamente (`miniaudio` fallisce silenziosamente) — i vocali Telegram vanno prima convertiti in wav 16kHz mono con ffmpeg, poi passati a whisper-cli.
- **[2026-08-21] [A278]**: `claude -p --output-format json` ha un solo canale di ritorno (il campo `result`, testo) — un turno che deve consegnare un allegato (un'immagine, un file) non ha modo di dirlo al processo che l'ha lanciato se non scrivendolo in un posto convenzionale che il chiamante controlla dopo (qui: `.claude_bridge_outbox/`, svuotata a ogni turno).
- **[2026-08-21] [A278]**: un layout SVG con testo posizionato a coordinate fisse relative a un solo margine (qui: etichette via a `y_top - N`) collide silenziosamente con qualunque altro elemento che condivide lo stesso spazio verticale (titolo/sottotitolo) — il codice non dà errore, il render sì. Va guardato il PNG vero, non solo verificato che l'SVG contenga le stringhe attese.
- **[2026-08-21] [A278]**: stessa famiglia di bug, asse orizzontale — una larghezza di colonna fissa (`ROUTE_WIDTH`) va bene finché i nomi sono corti; "A Route With a View" (settore Magoulias) sconfinava nella colonna accanto senza nessun errore. La colonna va dimensionata sul testo più largo della lista (stima caratteri×px, non serve una metrica di font vera), non su una costante.
- **[2026-08-21] [A278]**: un primo schizzo "corretto" (niente sovrapposizioni, dati giusti) non è lo stesso di uno schizzo *accettabile* — una linea dritta a zig-zag su sfondo piatto è stata bocciata a vista ("disegni orribili") pur essendo tecnicamente senza bug. Corretto cercando prima delle foto/topo reali già fatte (trovate solo foto panoramiche non annotate, i topo con le vie disegnate sono a pagamento su theTopo/27crags/Rockfax) e poi migliorando lo stile con elementi da guidebook vettoriale — silhouette di roccia irregolare seminata sul nome del settore (deterministica, non `random()` nudo), curve invece di segmenti dritti, rinvii, numerazione — invece di limitarsi a "i dati sono giusti quindi va bene".
- **[2026-08-21] [A276]**: ho scritto "un boot a freddo costa ~$0.12" (in un messaggio rivolto a Daniele, nel roadmap, e nel testo di `/new` del bridge) stimando token × prezzo API — ma Daniele è su abbonamento Claude Max (`stripe_subscription`, `hasExtraUsageEnabled: false`): nessun costo a consumo, tutto incluso nel piano flat. Prima di scrivere una cifra in € o $ legata all'uso di Claude Code, controllare `claude auth status` (`subscriptionType`/`billingType`) invece di assumere billing API.
- **[2026-08-21] [B340]**: `drop_pending_updates=True` su un bot che pilota una macchina remota **perde in silenzio** ciò che ti hanno scritto mentre il processo era giù, e il sintomo che arriva all'utente è «il bot è rotto» quando il bot è sano — rieseguire una coda vecchia è recuperabile, perderla no: consuma la coda e semmai rifiuta i messaggi vecchi **dicendolo**.
- **[2026-08-25] [B341]**: un vincolo UNIQUE `(user_id, session_date)` più `upsert(on_conflict=...)` su quella chiave codifica l'assunzione "un solo evento al giorno" direttamente nello storage — quando l'assunzione si rompe (un giorno con due falesie) l'upsert non fallisce e non avvisa, **sovrascrive silenziosamente** la riga precedente. Il bug era già in produzione e a un tap dal cancellare dati reali (sessione attiva non ancora finita) prima che venisse trovato dal vivo. Quando un vincolo UNIQUE + upsert modella "una entry per X", chiedersi esplicitamente se X è davvero sempre univoco nel dominio, non solo nei dati visti finora.
- **[2026-08-25] [B342]**: correggere un bug "una entry per data" in un solo punto non basta se il campo che quel bug corrompeva è letto/ricalcolato in più punti — `outdoor_load_score` aveva un secondo calcolo indipendente (`matching[-1]`) nel router che nessuna delle mie ricerche testuali su `outdoor_spot_name` avrebbe trovato, perché il bug non era nel nome del campo cercato ma nella stessa assunzione strutturale applicata altrove. Dopo un fix "una entry → tante entry", grep per **tutti** i punti che leggono quella tabella/log, non solo quelli toccati dal fix originale.
- **[2026-08-29] [B343]**: un'intera invariante di immutabilità (settimane passate) può vivere dietro **un solo** guard (`is_past_week` in `outdoor.py:89`) — comodo per un fix chirurgico (basta bypassarlo lì), ma vuol dire che nessun'altra difesa esiste a valle: `apply_events`/`complete_outdoor` sono deterministici sul plan che ricevono, senza alcuna protezione propria. Prima di bypassare un guard così, mappare *tutti* i suoi effetti collaterali (qui: il ripple sul giorno dopo) e non solo l'effetto che si vuole sbloccare.
- **[2026-08-29] [B343]**: `report_engine._find_week_plan` legge il cold store A221 solo `if user_id:` — per un utente anonimo/legacy (`user_id=None`) un report su una settimana già archiviata torna sempre vuoto, silenziosamente, invece di leggere l'archivio. Non toccato in questo brief (fuori scope, produzione ha sempre un user_id Clerk), ma è un gap reale da chiudere se mai serve un report per utenti anonimi.
- **[2026-08-30] [D280]**: in un audit multi-agente con ricerca online, il round di **verifica avversariale** (agenti istruiti a *confutare*, non a confermare) ha demolito 2 finding "critici" su 5 e ne ha declassati altri 2, trovando **citazioni misattribuite in tre casi su cinque** — un paper attribuito all'autore sbagliato, una review sulla popolazione generale usata per una tesi sul mantenimento della forza, una review sui determinanti di performance usata per un'affermazione sugli orizzonti temporali che non contiene. Un finding con file:linea e DOI *sembra* verificato e non lo è. Nessun output di agente di sintesi va riportato senza riaprire la fonte e il file.
- **[2026-08-30] [D280]**: il modo tipico in cui questo motore fallisce non è prescrivere male, è **scrivere in un campo che nessuno legge** — `freshness_policy` (4 scritture, 0 letture), `test_queue` (letto solo da `planner_v1`, che non è importato da nessuno), `recent_sessions` (nessun writer, 2 lettori), `deload_factor`, `unmet_stimulus`, e i `domain_weights` personalizzati ma non consumati da `planner_v2`. Ogni volta il codice *sembra* implementare la regola. Dopo aver aggiunto un campo di stato o una policy, verificare con grep che esista almeno un consumatore vivo — e che il modulo che lo consuma non sia legacy.
- **[2026-08-30] [D280]**: prima di riportare a Daniele numeri letti dallo stato di produzione, controllare se il campo è **troncato per design**: `feedback_log` è un buffer rotante di 7 (`adaptive_replan.py:150`), quindi "solo 7 feedback su 98 sessioni" non è un segnale di nulla. Stesso errore evitabile su `session_completion_log`, dove il conteggio reale (87 done / 11 skipped) diceva l'opposto dell'impressione data dagli ultimi record letti a occhio.
- **[2026-08-30] [B344]**: verificare che il codice *faccia* quello che dici non basta — `step_grade('7a+',-1) → '6C'` era riproducibile ed è comunque diventato un finding sbagliato, perché non avevo letto `vocabulary §2.10.1`, che documenta la scala a gradi interi **anche per il lead** e rende quell'output corretto per progetto. Prima di chiamare bug un comportamento, cercare la spec e il test che lo blindano (`test_step_grade_no_plus_in_output` esisteva): se un comportamento ha un test dedicato che lo afferma, è una decisione, non un incidente.
- **[2026-08-30] [B344]**: cambiare un valore di `DEFAULT_ADJUSTMENT_POLICY` NON raggiunge tutti gli utenti — `_rule_midpoint_pct` preferisce `working_loads.rules.adjustment_policy` salvata nello stato. Corollario sui test: le fixture che includono una policy esplicita sono **immuni** al cambio di default, quindi "il test passa" non prova che il fix funzioni. Gli unici due test che si sono rotti erano gli unici due con `rules: {}` — cercare quali fixture esercitano davvero il percorso prima di concludere.
